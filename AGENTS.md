# AGENTS.md

The actual fast-start context for this repo lives in [`CLAUDE.md`](./CLAUDE.md). It is kept as the single source of truth — read that file first.

`CLAUDE.md` contains:
- Project overview (what this is + who it's for)
- Current state (last shipped PR, recent regressions)
- Hard rules
- Triage table — which deep doc to read for which task type
- Codebase map
- Known landmines

This file exists as a thin shim so tooling that follows the `AGENTS.md` convention (Codex etc.) lands here and gets redirected.

## Non-Claude tooling: read this then go

If your runtime resolves the markdown link above automatically, follow it. Otherwise:

```
/Users/jiangwei/Claude/Chinese/CLAUDE.md
```
