/**
 * The single unified art style for ALL generated raster art (word pictures +
 * collectible card art), so the whole game looks cohesive. David approved this
 * cartoon look from a sample on 2026-06-14.
 *
 * Prepend to a per-subject description, e.g. `${UNIFIED_ART_STYLE}a red panda`.
 * Voyage MAP backdrops deliberately use their OWN recipe (they're landscapes /
 * sea-charts, not single-subject), so they don't import this.
 */
export const UNIFIED_ART_STYLE =
  "children's book illustration, friendly modern cartoon, bold clean outlines, " +
  'bright flat colors with soft simple shading, single centered subject, ' +
  'plain soft pastel background, no text, no watermark: ';
