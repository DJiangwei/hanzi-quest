# CHANGELOG — the full per-PR shipping log

> Moved verbatim out of CLAUDE.md on 2026-07-03 (roadmap item B1) so the auto-loaded context stays lean. This is the deep archaeology: per-PR narratives, design journeys, and the context in which each landmine was learned (the landmines themselves also live, grouped, in CLAUDE.md — that copy is canonical). **New PRs:** add a one-line row to PLAN.md §1's shipped table and a full entry here; CLAUDE.md only gets its current-state snapshot updated + any new landmine.

---

## Current state (last refreshed 2026-07-03)

**Shipped:** PR #1 → #45. The product is end-to-end playable in production: weekly authoring, AI scene generation (DeepSeek V4 Pro, **V2 schema with per-word imageHook**), **real Vercel Blob cartoon images on image_word scenes for all 426 words** (`words/{wordId}.png`, 512×512, `words.image_url` fully backfilled as of 2026-06-07), **multi-map chart system** with **Map 1 = 加勒比海 / Caribbean Sea** (10 weeks published) and **Map 2 = 印度洋 / Indian Ocean** placeholder (locked card on `/maps`), shared `pirate-class-level-1` curriculum pack semantics, **kid-first surface**: root `/` auto-redirects single-child accounts to `/play/[childId]` and parent dashboard sits behind a 4-digit PIN gate, **bottom 4-tab nav** (🏝️ Map / 🎒 背包 / 📅 日历 / 🛒 商店 + ⚙️ gear), **MapHeaderPill** (📍 加勒比海 ⬇) on home links to a `/maps` gateway, **View Transitions morph** island → hub, **island map → week hub → 3 separately-enterable sections** (回顾 / 练习 / Boss战), **9 active scene types + boss** (pinyin_pick retired; image_word added), coins (with daily-login / streak-milestone / perfect-week bonuses + a `BonusToast` HUD layer, **plus streak-freeze auto-burn**), **7-day check-in strip + month-grid `/calendar`** page, **3 consumable powerups** (💡 Hint / ⏭️ Skip / 🧊 Streak-Freeze) accessible via a bottom-right tray on every scene, **5 collection packs** in the renamed **Backpack** (`/play/[childId]/collection` with "最近获得 / Recently Obtained" strip): zodiac (12), flags (30), sea creatures (20), dinosaurs (15), solar system (10). **Shop hub** (`/play/[childId]/shop` → **5 of 5 tabs live**: Avatar, Sounds, Pet, Decor, Powerup). Layered SVG avatar in play HUD, animations + audio + treasure-map cards, PWA manifest.

**PR #21–#27 (just shipped, 2026-05-19):**
- PR #21 (spec doc) + #22 (impl) — Shop hub + Avatar cosmetics. `/play/[childId]/shop`, `ShopHudButton` mounted in play layout, `AvatarRender` in island-map header, ~20 seeded items. New: `src/lib/db/shop.ts`, `src/lib/actions/shop.ts`, `src/lib/errors/shop-errors.ts`, `src/lib/avatar/{defaultLook,itemCatalog}.tsx`.
- PR #23 (spec doc) + #24 (impl) — Collector's Atlas hub + Flags pack. `/collection` is now `AtlasHub` (multi-pack lobby); `/collection/[packSlug]` is generic. New: `src/lib/collections/{packRegistry,flagsData}.ts`, `src/components/play/{AtlasHub,AtlasHallCard,PackGrid,PackPageBody}.tsx`, `src/components/play/items/{FlagCard,ZodiacGridItem}.tsx`. 30 country flags seeded into prod via `scripts/seed-flags-pack.ts`. Per-pack gacha cost from `getPackMeta(slug).paidPullCost` (zodiac=500, flags=300).
- PR #25 — Sea Creatures pack (20 bilingual ocean creatures, pirate-themed). `src/lib/collections/seaCreaturesData.ts` + `src/components/play/items/SeaCreatureCard.tsx`. Habitat instead of capital. cost=300.
- PR #26 — Dinosaurs pack (15 dinosaurs across Triassic/Jurassic/Cretaceous eras). Only 2 emojis exist (🦖/🦕), so `DinosaurCard` color-codes the card by era + renders a bilingual era badge for differentiation. cost=300.
- PR #27 — Solar System pack (8 planets + Sun + Moon = 10). Mostly coloured-disc emojis (🔴/🟠/🟡/🔵/🟣/⚪) plus the only real ones (🪐/☀️/🌝/🌍). `SolarBodyCard` colour-codes by body type (rocky/gas/ice/star/moon) + bilingual type badge. cost=300.
- PR #28 — Coin economy expansion. 3 new enum values on `coin_reason` (drizzle 0005): `daily_login` (+20, idempotent per UTC date), `streak_milestone` (+100 every 7 days, hooks the previously-empty `streaks` table via `tickStreak`), `perfect_week` (+200 when every scene of a week has at least one score=100 attempt; idempotent per week). New: `src/lib/db/streaks.ts` (tickStreak + todayUtcIso), `src/lib/db/play.ts isPerfectWeekForChild`, `src/components/play/BonusToast.tsx`. Actions return `bonuses: EconomyBonus[]` that the SceneRunner pipes into the toast layer.
- **PR #30 (just shipped, 2026-05-22)** — 4-segment weekly structure + 3 new scene types. `compileWeekIntoLevels` rewritten to emit `review → sound → sight → meaning → boss` blocks; new `pinyin_pick`, `translate_pick` (bidirectional CN↔EN), `sentence_cloze` scenes (all delegate to `MultipleChoiceQuiz`); boss question rotation widened from 3 → 6 types (word_match still excluded — it's a multi-character round); bilingual segment chip rendered in `SceneRunner` from each level's `sceneConfig.segment`. One-off `scripts/recompile-all-weeks.ts` recompiled all 10 published weeks in prod. Now **9 scene types + boss**.
- **PR #31 (just shipped, 2026-05-23)** — Sound / FX themes. 4 procedural Web-Audio themes (`music-box`, `retro-arcade`, `nautical`, `fanfare-plus`) live in the Sounds shop tab. New `child_settings` table (PK childId) stores `sound_theme_slug`; `setAudioTheme(slug)` swaps the runtime handler registry; `SoundThemeBootstrap` in the play layout hydrates it on mount. Shop card has 🔊 preview button. `scripts/seed-sound-themes.ts` seeds 4 `shop_items` rows. Equip is optimistic + immediate (no page reload).
- **PR #32 (just shipped, 2026-05-23)** — Achievements / Trophies. 20 trophies in 5 categories (mastery/streak/collection/coins/practice). New `trophies` + `child_trophies` tables (migration 0008) + `trophy_category` enum. `checkAndGrantTrophies(childId, context)` is the single grant API, wired into `finishAttemptAction`, `finishLevelAction`, `pullPaid`, `equipSoundThemeAction`. New `/play/[childId]/trophies` route reached via a 6th hall card on the Atlas Hub. `TrophyToast` (gold accent, animate-bonus-pop) renders alongside `BonusToast` in `SceneRunner`. One-off `scripts/backfill-trophies.ts` retroactively grants trophies — already ran for Yinuo (8 trophies inherited from existing play history).
- **PR #33 (shipped 2026-05-23)** — Static pet companion. 8 pets (parrot/crab/ship cat/monkey/sea turtle/dolphin/bat/glow jellyfish) in the new Pet shop tab, 300–1200 coins. Equipped pet renders beside the avatar on the island map only; tap → random bilingual speech bubble for ~2.5s. New tables `pets` + `child_pet_equipped`; pets seeded into `shop_items` via slug join; ownership derives from `shop_purchases`. `equipPetAction` validates ownership.
- **PR #34 (shipped 2026-05-24)** — Island decorations + shop purchase-dispatch fix. 10 hand-rolled SVG decorations (sailboat / seagull pair / hibiscus / fish school / compass rose / rainbow / pirate flag / whale tail / lighthouse / treasure chest, 200–1200 coins, total set ≈ 5,100) auto-appear at fixed anchors on the island map once owned (no equip UI; purchase = visibility). New `decorations` table + `'decor'` enum value (migration 0010). Anchors live in TS (`src/lib/decor/anchors.ts`), DB stores only the anchor slug. New `DecorTabBody` shop tab (now 4 of 5 live; Powerups remains queued). 2 new trophies: `decor-starter` + `decor-completionist`. **Also a regression fix**: `purchaseShopItemInTx` previously hardcoded `kind === 'avatar'` and silently blocked pet/sound-theme purchases at the SQL boundary — refactored to a `switch (kind)` dispatch. All 4 live shop tabs are now actually purchasable.

**Most recent regressions fixed:**
- PR #18 — `finishLevelAction` / `listWeekChars` used `getWeekOwnedBy` which fails for shared-pack weeks (parent_user_id NULL). Fixed by switching to `getPlayableWeekForChild`.
- PR #20 — flashcard hanzi was 14rem fixed → now `clamp(11rem, 55vw, 22rem)`; `ZodiacIconDefs` (SVG `<symbol>` defs) only mounted on `/collection`, broke chest reveal — now mounted in the play layout.
- PR #34 — `purchaseShopItemInTx` was avatar-only since PR #21; PRs #31 (sounds) and #33 (pets) shipped UI that called the action but it threw at the SQL boundary. Fixed via switch-kind dispatch — see the new "shop purchase dispatch" landmine below.

- **PR #35 (shipped 2026-05-24)** — Week hub restructure. Each week now splits into 3 separately-enterable sections via a new hub at `/play/[childId]/week/[weekId]` (sections: 回顾 / 练习 / Boss战, served at `/play/[childId]/level/[weekId]/[section]`). Old `/level/[weekId]` URL redirects to the hub. Boss locked until ≥6/12 practice scenes cleared. Practice quantity ~2× (5 → 12 scenes per 10-char week: 3 audio_pick + 3 sight + 6 meaning). `pinyin_pick` retired from compile + boss rotation (template flipped to is_active=false; component file retained for backwards compat). New `week_levels.level_key` column + unique constraint enables stable upserts — future compile changes preserve `scene_attempts.week_level_id` linkage. One-time progress reset accepted at ship time. All 10 prod weeks recompiled.

- **PR #36 (shipped 2026-05-24)** — Image-prompted word formation (text-stimulus, image-ready). New `image_word` scene type woven into the sight segment: shows a DeepSeek-generated kid-friendly description card + base 字 chip + 4 candidate 词. AI scene-gen pipeline extended to V2 (per-word `imageHook` added to `WeekContent`; persisted on new `words.image_hook` column, migration 0012). Practice grows 12 → 14 scenes per full week (2 image_word slots); boss unlock threshold bumps 6 → 7. Backfill script populated `image_hook` for all 238 existing prod words (~$0.10 DeepSeek). All 10 prod weeks recompiled (8 × 25 levels, 2 × 19 levels). Real image generation deferred (PR #38 territory once Vercel Blob + AI Gateway are provisioned).

- **PR #37 (shipped 2026-05-25)** — Consumable powerups + Shop 5-of-5 live. 3 powerups in a bottom-right floating tray on every scene: 💡 Hint (30c, gray-out 1 wrong choice, no penalty), ⏭️ Skip (100c, advance without scoring), 🧊 Streak-Freeze (200c, auto-burn on missed day). Starter pack (1 hint + 1 skip) auto-granted on first play when `powerup_inventory` is empty. New `'skip'` value in `powerup_kind` enum (migration 0013); reuses existing `powerup_inventory` table. `tickStreak` extended with freeze auto-burn branch — preserves currentStreak + emits a BonusToast "🧊 Streak saved!". Purchase dispatch (PR #34 switch) gained `case 'powerup'` (stackable, no AlreadyOwnedError). Hint mode in `MultipleChoiceQuiz` is opt-in via optional prop. Skipped scenes write `scene_attempts(score=0)` — don't count toward boss-unlock. Shop is now 5 of 5 live tabs.

- **PR #38 (shipped 2026-05-26)** — Kid-first surface refresh. Five subsystems in one PR: (1) root `/` auto-redirects signed-in single-child accounts to `/play/[childId]`; (2) bottom `KidNavBar` (🏝️ Map / 🎒 背包 / 📅 日历 / 🛒 商店 + ⚙️ gear) mounted in play layout, mid-scene tap shows quit-confirm dialog via `MidSceneProvider` context; (3) parent dashboard gated behind 4-digit PIN (bcrypt, HttpOnly 15-min cookie, 5-attempt → 5-min lockout, `?reset=1` recovery) — new `parent_settings` table (migration 0014), new `/parent/unlock` page + `POST /api/parent-unlock`; (4) View Transitions API morph island circle ↔ hub hero via matching `viewTransitionName: island-${weekId}`; new `loading.tsx` Suspense skeleton; (5) 7-day check-in strip on home + month-grid `/calendar` page driven by `getActivityForRange` aggregation over `coin_transactions`; (6) Atlas renamed to **背包 / Backpack** with 🎒 header + "最近获得 / Recently Obtained" strip showing last 3 acquisitions across collection / avatar / pet / decor with a 24h NEW sticker (server-render snapshot via `nowMs` prop). New dep: `bcryptjs`. 20 new tests across 12 new test files (485 → 505).

- **PR #40 (just shipped, 2026-05-27)** — Multi-map chapter system. Sea-region theme: existing 10-week `pirate-class-level-1` pack renamed to **加勒比海 / Caribbean Sea** (Map 1); new **印度洋 / Indian Ocean** placeholder pack (slug `pirate-class-level-2`, 0 weeks) seeded as a real row → renders as locked card on `/maps`. New bilingual `name_zh` + `name_en` columns on `curriculum_packs` (migration 0015). New `/play/[childId]/maps` gateway page (`MapsHub` + `MapCard` with 3 visual states: current / switchable / locked). New `MapHeaderPill` (📍 加勒比海 ⬇) on home, between avatar header and WeekStrip. `switchMapAction` updates `child_profiles.current_curriculum_pack_id`; `revalidatePath('/play/[childId]')` flushes home cache after switch. `MapLockedError` (pure, client-safe) thrown when target pack has 0 weeks. `KidNavBar` 🏝️ Map tab now active on `/maps`. 20 new tests, 525 total. `clearedCount` per-pack is wired to 0 in MapForChild — deferred (counting cleared weeks per pack will require joining `progress` table, minor follow-up).

- **PR #42 (shipped 2026-05-28)** — Pollinations.ai image backfill + authoring integration. Migration 0016 adds nullable `words.image_url`. New `src/lib/ai/pollinations.ts` builds the URL with a fixed cartoon-kid style preamble (`'cartoon illustration for children, bright colors, simple, single subject, no text:'`) + `model=turbo&width=512&height=512&nologo=true&seed=<deterministic-from-wordId>` and uploads PNG bytes to Vercel Blob at `words/{wordId}.png` (stable URL: `addRandomSuffix:false, allowOverwrite:true`). New `'use server'` action `generateMissingImagesForWeek(weekId)` (per-word error-isolated, returns counts) is called in a try/catch from both `generateWeekContent` + `regenerateCharacter` — image-gen failure cannot block a week from reaching `awaiting_review`. `ImageWordScene` renders `<img>` stimulus when `image_url` is set; falls back to the existing text card otherwise (alt text = `imageHook ?? meaningEn ?? text`). `imageUrl` flows runtime-only through `getCharactersWithDetailsForWeek` → section page `pool` mapper → `SceneRunner` → scene (NOT through `scene_config` / `compile-week.ts` — no recompile needed). New `scripts/backfill-word-images.ts` for the existing prod words (idempotent, mirrors the `backfill-word-image-hooks.ts` pattern). Pollinations is free + key-less so cost is $0. 17 new tests across 3 new test files, 542 total. New dep: `@vercel/blob`.

- **PR #44 (shipped 2026-05-28)** — Pollinations free-tier fix #1. Initial backfill attempt yielded 0/426 because Pollinations had moved `model=flux` and `enhance=true` to paid tier (HTTP 402). Switched to `model=turbo` (still free) and dropped `enhance=true`. Slightly less stylized output but serviceable at 192px. Also added `scripts/verify-image-url-column.ts` — a read-only operational tool that confirms migration application + reports backfill progress (total/NULL/eligible counts). Used to discover this regression and useful for future migration checks.

- **PR #45 (shipped 2026-05-28)** — Pollinations free-tier fix #2 (concurrency). After PR #44, backfill still failed 0/426. The 402 response body actually said `"Queue full for IP: ... 1 requests already queued (max: 1)"` — Pollinations free tier enforces **1 concurrent request per IP**. Dropped `CONCURRENCY` from 10→1 in `scripts/backfill-word-images.ts` and 5→1 in `src/lib/actions/images.ts`. Trade-off: backfill ~3 min → ~30-40 min for 426 words (one-time); authoring image-gen ~10s → ~50-100s per week (non-blocking, doesn't delay `awaiting_review`).

**Image backfill status as of 2026-06-07:** COMPLETE. All 426 words with `image_hook` have populated `image_url`; `pnpm tsx scripts/verify-image-url-column.ts` reports `words with image_url NULL: 0`, and an HTTP `HEAD` sweep verified 426/426 Blob URLs are reachable. The remaining rows were generated by Codex's built-in image model per David's updated high-recognition cartoon direction. See `docs/codex-image-backfill.md` for the handoff/audit trail.

- **PR #48 (shipped 2026-05-31)** — **Story Mode**: Yinuo-as-protagonist generative weekly narrative. New `story_chapters` table (migration 0017, one row per child×week, UNIQUE-constrained); DeepSeek (`@ai-sdk/deepseek` + `generateObject`) generates a bilingual chapter (40-60 EN words + 2-3 short ZH sentences) on boss-clear; boss-score-weighted tone (`triumphant` ≥95 / `narrow_escape` <67 / `standard` else). Hero card composes existing `AvatarRender` + equipped pet via `PetCompanion`. Surfaces: home page `LatestChapterPill` (only when unread), Backpack `Story Library` hall card → archive at `/play/[childId]/collection/story-library`, chapter view at `/play/[childId]/story/[weekId]`. Web Speech API zh-CN audio button on every chapter (`useSyncExternalStore`-based detection, free + no infra). Eager fire-and-forget gen in `finishLevelAction` (via `triggerEagerStoryGeneration` async helper), synchronous fallback on chapter page if missing. Boss section page exits to story page after win (changed `exitHref` for `section === 'boss'`). Trophy: `first-chapter` in new `story` category. Cost ~$0.0004/chapter (~$0.05/year per child). Test count delta to be backfilled post-merge.

- **PR #50 (shipped 2026-05-31)** — Scene voice/TTS. Three shared client primitives: `useSpeechSupported` hook (SSR-safe Web Speech API detection via `useSyncExternalStore`), `useSpeak` hook (cancels in-flight + builds `SpeechSynthesisUtterance` with `lang='zh-CN'` + `rate=0.85` + try/catch), and `SpeakButton` component (sm icon-only variant + md labeled-CTA variant, both ≥44×44 tap target, `focus-visible` ring, renders `null` when API absent). FlashcardScene + AudioPickScene refactored to drop their inline `speak()` helpers (which used the buggy `'speechSynthesis' in window` check — PR #48 landmine). New: `<SpeakButton size="sm">` next to the hanzi stimulus on TranslatePickScene `cn_to_en` direction only. ImagePickScene + VisualPickScene **deferred to v2** (their stimulus IS the answer surface — choices are hanzi/pinyin, so speaking the stimulus reveals the answer; needs post-reveal pattern). ChapterAudioButton refactor (originally Task 4) **deferred to post-PR-#48 follow-up** — file doesn't exist on this branch since PR #48 hasn't merged. No DB migration, no recompile, no server change. +15 tests across 4 new test files (542 → 557 total).

- **PR #51 (shipped 2026-05-31)** — Playtest polish + bug fix bundle from David's post-PR-#50 session with Yinuo. Six items: (1A) flashcard hanzi shrunk `clamp(11rem, 55vw, 22rem)` → `clamp(8rem, 42vw, 16rem)` (~27% smaller, swapped from inline `style` to Tailwind class `text-[clamp(8rem,42vw,16rem)]` so the regression test could catch reverts in jsdom). (1B) Flashcard gains example-word + example-sentence reveal rows, each with `<SpeakButton size="sm">` from PR #50, derived from existing `character.firstWord` and `character.sentence?.text` (no new DB fields). Also fixed a latent bug in `getCharactersWithDetailsForWeek`: word query lacked `ORDER BY position` → `c.words[0]` was non-deterministic. Added `.orderBy(asc(characterWord.position))`. (2A) `visual_pick` retired — `scripts/retire-visual-pick.ts` flips `scene_templates.is_active=false`; compile-week drops the slot AND its 3 fallback paths (no more visual_pick as image_pick / image_word fallback); sight slot count 3 → 2; total practice 14 → 13; `BOSS_UNLOCK_PRACTICE_THRESHOLD` 7 → 6; `PRACTICE_SCENE_COUNT` 14 → 13. `VisualPickScene.tsx` component + config schema + SceneRunner case all stay for backwards-compat with old `scene_attempts` rows (same pattern as PR #35's `pinyin_pick` retirement). (2C) `ImageWordScene` prompt drops the base-hanzi chip — new prompt "看图选词 / Match the picture to a word"; `baseChar` prop kept but unused (destructured as `_baseChar`). (3A) Boss chest button hides on REPEAT clears — `finishLevelAction` return extended with `freePullClaimed: boolean` from `existing?.freePullClaimed`; SceneRunner captures it in `useState` and `chestAvailable = lastSceneType === 'boss' && !freePullClaimed`. Previously chest button rendered on every boss clear, then 422'd as `AlreadyClaimedError` server-side. (5A) Sounds-tab preview was silently no-op'ing because `void ctx.resume()` didn't await — `theme.ding(ctx)` ran on a still-suspended AudioContext. Fixed: `preview` is now async, `await ctx.resume()` before `theme.ding(ctx)`; onClick wraps in `void`. Recompile required (`pnpm tsx scripts/recompile-all-weeks.ts`) + retire script run post-merge. +14 tests across 3 new test files (557 → 571, branch-relative; if PR #50 hasn't merged then absolute against main is 542 → 571 with PR #50's 15 + PR #51's 14).

- **PR #52 (shipped 2026-05-31)** — Gacha economy redesign. Removed coin-purchased shop gacha entirely (`GachaPullButton` + `pullPaid` action marked `@deprecated`, kept one release). Cards now flow from three play-to-earn channels: Boss clear (including REPEATS — refId=sessionId so each clear is independent, contradicting PR #51's chest-hide), `perfect_week` first-award, and Story chapter first-read. Weekly cap 10 cards/UTC-week tracked in new `child_card_grants_weekly` table (PK childId + weekStartUtc, upsert with `onConflictDoUpdate` for race safety). Idempotency in new `card_grants_log` table (PK childId + source + refId; 23505 catch). Duplicates auto-grant 1 shard of the picked pack's `shard_balances`; new `SwapDialog` lets kid spend 3 shards on any chosen unowned card of the same pack. New DB layer `src/lib/db/grants.ts`: pure `weightedRandomPick` (weight = `dropWeight × (1 + packUnowned)`; throws on zero-total catalog rather than silently fall back), plus `pullCardInTx` + `swapShardsInTx` transactional helpers. New actions `pullCardForChild` (trust-caller — no requireChild) + `swapShardsForItem` (user-tappable — requireChild). PR #51's `freePullClaimed` chest-hide reverted in `SceneRunner` (`chestAvailable = lastSceneType === 'boss'` unconditional); `freePullClaimed` column stays in DB (additive). New `ShardPill` (🔹 N) on pack page header + ×N dupe count badge on owned items. `LevelFanfare` extended with `cardGrant` prop showing "🎴 新卡片！", "+1 碎片", or "今天的卡片满了" (cap reached) banners. perfect_week pullCardForChild is fire-and-forget with `.catch()` log (boss-clear awaits). Migration 0018 adds 2 tables. Recompile NOT required (gacha changes don't touch week_levels). Post-merge: standard PR + auto-migrate on Vercel build. +34 tests across new test files (619 → 653 total).

- **PR #57 (shipped 2026-06-02)** — 连连看 (Lianliankan) replacement for WordMatchScene. The old `word_match` "match these N chars to these N words" multi-pick was confusing (per David's playtest) and pedagogically too hard. Replaced with a Lianliankan puzzle in sight slot 1: 6×4 board with 1-cell empty border, inner 4×2 playable = 4 pairs of (hanzi tile ↔ English-meaning tile). Tap two matching tiles → if connectable by ≤2 right-angle bends through empty cells, clear both with animated SVG connecting line; if same pair but blocked → shake + deselect (no penalty); if different pair → buzz + shake + deselect. Pure path-finding library at `src/lib/scenes/lianliankan.ts` (BFS with state `(row, col, dir, bends)`) fully tested independently of UI. New `'lianliankan'` scene_type enum value (migration 0019) + seed in `scripts/migrate.ts`. `word_match` template flipped to `is_active=false` via new `scripts/retire-word-match.ts` (same pattern as `pinyin_pick` PR #35 + `visual_pick` PR #51). `WordMatchScene` component + `WordMatchConfigSchema` + SceneRunner case all retained for backwards-compat. Auto-shuffle on deadlock (no valid pair); 💡 hint highlights one valid pair via pulse. Tile size `clamp(48px, 14vw, 72px)`. `buildInitialBoard` ensures ALL 4 pairs are initially reachable (stricter than spec's `hasValidPair` — implementer judgment call to avoid 1-pair-stuck boards). Recompile required post-merge. +28 tests across 4 new test files (659 → 687 total: lianliankan-pathfinding ×9, lianliankan-board ×9, lianliankan-scene ×6, retire-word-match ×1, plus 4 compile-week tests in existing files minus 1 deleted useEffect test).

- **PR #58 (shipped 2026-06-03)** — Avatar expansion + multi-theme. Expanded from 4 slots (head/hat/top/background) to **7** (added hair, pants, decor). `AvatarRender` now iterates `AVATAR_SLOT_IDS` instead of hardcoding 4 layers — adding a new slot is a single-array edit. Layering order back→front: background → decor → head → pants → top → hair → hat. New `theme` text column on `avatar_items` (migration 0020, nullable, indexed); existing 22 items backfilled to `'pirate'` via `scripts/migrate.ts` seed pass. **Two themes in v1**: pirate (24 original + 6 new shop, all hair/pants/decor coverage) and caribbean (11 new across all 7 slots, including beach-sunset background). 2 new defaults: `default-hair-brown`, `default-pants-blue`. `decor` slot has NO default — kid sees no decor until they buy one (intentional, expressive). New `ThemeChipStrip` component (`[All] [Pirate] [Caribbean]`) at the top of the avatar shop tab, client-side filter via `useState`. Items mix-and-match across themes (no theme lock). `AVATAR_THEMES` is a `text` (not pgEnum) so future themes (Indian Ocean, Space, Exploration) are code-only additions. Total catalog: 22 → 41 items. +17 tests across 4 new test files (687 → 704 total).

- **Card Economy v2 (PR open 2026-06-04)** — three changes to the PR #52 gacha system, from David's playtest. (A) **Visible swap chip**: the shard-swap was only reachable by tapping a grayed-out locked card (nobody discovered it; 小板 had 14 unused shards). `PackPageBody` now renders a `🔹换卡 / Trade` chip on every unowned card (enabled at ≥3 shards, `需 3🔹` greyed below) + a header hint; the whole-card invisible tap target was removed. (B) **Daily cap replaces weekly cap**: new `child_card_grants_daily(childId, dayUtc, count)` table (migration 0021) + `DAILY_CARD_CAP = 10`; `pullCardInTx` reads/writes the daily counter keyed by `dayUtc`; `pullCardForChild` passes `todayUtcIso()`. Result fields renamed `cardsThisWeek→cardsToday`, reason `weekly_cap_reached→daily_cap_reached`. `child_card_grants_weekly` + `WEEKLY_CARD_CAP` are now **dead but kept** (append-only). (C) **Weekly check-in 大礼包**: `grantGiftPackInTx` grants **one card per active pack** (5 today, derived not hardcoded), **bypassing the daily cap**, idempotent per UTC week via `cardGrantsLog(childId,'weekly_checkin',weekStartUtc)`. Triggered by `claimWeeklyGiftIfDue` inside `finishAttemptAction` only when `awardDailyLoginIfDue` reports a fresh check-in; fires when distinct check-in days this UTC week ≥ `WEEKLY_CHECKIN_THRESHOLD (5)` (counted via `getActivityForRange`'s `dailyLoginBonus`). Surfaced via new `GiftPackReveal` modal (mounted in `SceneRunner` from the `finishAttemptAction.giftPack` field) + a `🎁 本周签到 N/5` hint on the home `WeekStrip`. New: `src/lib/db/checkins.ts` (`countCheckInDays`), `src/components/play/GiftPackReveal.tsx`. +30 tests (704 → 734). No recompile. Migration 0021 auto-applies on deploy.

- **Per-Week Boss Animations (PR #64, open 2026-06-05)** — first of 3 PRs David requested (then post-reveal card polish, then Daily Quests + Season Pass). Replaces the single shared kraken with **10 bespoke procedural-SVG sea creatures**, one per week, each with `intro / idle / damage / defeat` animations (framer-motion, reduced-motion → static per-state poses). New shared `BossCreatureProps` interface (`src/components/scenes/fx/bosses/types.ts`); `boss-roster.ts` registry with `getBossCreature(weekNumber) = ROSTER[(weekNumber-1) % 10]` (client-only — holds React components); 10 creature components under `src/components/scenes/fx/bosses/` (Kraken/Crab/Anglerfish/SeaSerpent/Shark/JellySwarm/ElectricEel/GiantClam/SeaDragon/Whirlpool). `BossScene` gained a `weekNumber` prop + a phase machine: intro on mount (`INTRO_MS=1200`) → idle → transient `damage` flinch (`DAMAGE_MS=400`) per correct answer → `defeat` (`DEFEAT_MS=1200`) → **then** `onComplete(true)` (victory is now DELAYED by a timer, not synchronous). `weekNumber` threaded section page (`week.weekNumber`) → `SceneRunner` (optional prop, default 1) → `BossScene`. Old `BossKraken.tsx` + its test deleted (superseded by `bosses/Kraken.tsx`). **Also a checkpoint bug fix:** removed the redundant legacy "开启宝箱" chest button + `pullFreeFromBoss` path from `LevelFanfare` — since PR #52 the boss card is auto-granted by `pullCardForChild('boss_clear')` and shown via the `cardGrant` banner; the old manual chest threw "开宝箱失败". `TreasureChestReveal` + `pullFreeFromBoss` kept (the next PR reuses the reveal). No DB change, no recompile.

- **Voyage Board / procedural treasure-map home UI (PR #65, 2026-06-06)** — replaced the procedural `IslandMap` (for packs with a configured voyage) with a **procedural treasure-map "voyage board"**: a tall vertical-zigzag snake of big numbered medallions (①…⑩) on a dotted route, styled as a parchment sea-chart (cream parchment frame, scalloped teal wave border on all 4 edges, sea-wave texture, corner ornaments 🐚🧭⚓⭐, title ribbon). Each stop = a landmark emoji + gold number badge + bilingual name; states: cleared 🏴, current ⛵ + pulse, locked 🔒. New: `src/lib/play/voyage-layout.ts` (`voyageLayout(total)` → vertical-zigzag `{xPct,yPct}[]`, board height = `stops × STOP_GAP_PX(210)`), `src/components/play/VoyageBoard.tsx`. Reshaped `src/lib/play/map-boards.ts` → `VOYAGE_MAPS` (per pack `{nameZh,nameEn,stops:[{labelZh,labelEn,emoji}]}`, Caribbean 10 / Indian Ocean 9) + `getVoyageMap(slug)`. Home page chooses VoyageBoard when `getVoyageMap(currentMap.slug)` is non-null, else `IslandMap` (unchanged fallback). **Design journey** (3 rejected iterations): static 1:1 AI-illustration + invisible hotspots (`MapBoard`+WebP) → too small / no numbers / image-locked; 16:9 serpentine → too short on phone; landed on the tall vertical snake (original IslandMap layout) scaled up + sea-chart CSS. The AI map `.png`s stay in `public/maps/` **only as design reference** (the `.webp` + `MapBoard` were deleted). CSS approximates the hand-drawn look; a true illustrated parchment frame is deferred until a paid image-gen provider is enabled. No DB change, no recompile.

**Landmine:** *Voyage maps are client-only config.* `src/lib/play/map-boards.ts` exports `VOYAGE_MAPS`/`getVoyageMap`; `VoyageBoard` takes a `packSlug: string` and resolves it itself — never pass the config object from a server component (PackUiMeta-style RSC hazard). *Stop↔week pairing is positional + auto-laid-out:* `islands[i]` (by weekNumber) occupies `voyageLayout(stops.length)[i]`; stops beyond the published-week count render locked 🔒. Adding a pack's voyage = add a `VOYAGE_MAPS[slug]` entry (name + ordered landmark emojis); absence falls back to `IslandMap`. No coordinates — `voyageLayout` positions everything; board height scales with stop count (`STOP_GAP_PX`).

- **Post-Reveal Card Polish (PR open 2026-06-06)** — unified all 3 card-grant sources (boss-clear, perfect-week, weekly 大礼包) into one **tap-to-open `CardChestReveal`** (chest → tap → shake/open → card with name + lore + glyph; sequential for multi-card; dupe → shard note; reduced-motion shows card directly). Threaded card display fields (`slug/nameZh/nameEn/loreZh/loreEn`) through `CardGrantResult` + `GiftCard` (richer `collectibleItems` SELECT, no schema change); `finishLevelAction` now awaits perfect-week + returns `cardGrants: RevealCard[]`; `SceneRunner` routes all sources into a `revealCards` queue. Retired `GiftPackReveal` + the `LevelFanfare` card banner. New `src/lib/play/reveal-card.ts`, `src/components/scenes/fx/CardChestReveal.tsx` (reuses `TreasureChestReveal`). No DB change, no recompile.

- **XP Foundation + Daily Quests (PR open 2026-06-06, retention Phase 1)** — first slice of the Daily-Quests+Season-Pass retention layer. New **XP currency** (separate from coins — never converts) + a **cosmetic player Level + bilingual title** (`levelForXp`/`titleForLevel` curve in `src/lib/xp/levels.ts`; `awardXp` ledger in `src/lib/db/xp.ts`; tables `child_xp` + `xp_events`, migration 0022). **3 daily quests** per UTC day (`src/lib/quests/definitions.ts` — 8 templates incl. `earn_card` which replaced the obsolete coin-gacha quest), generated idempotently on home-page render (`generateDailyQuests`), progress filled by `tickQuestProgressSafe` ticks wired **additively + guarded + fire-and-forget** into `finishAttemptAction` (scene/perfect/practice/review), `finishLevelAction` (boss/full_level/earn_card), `purchaseShopItemAction` (spend_coins), `claimWeeklyGiftIfDue` (earn_card), and the streak-milestone branch (+100 XP). All-3-done → `claimDailyChest` coin chest (50–100 coins + 30 XP, idempotent via `daily_quest_chests` PK). UI: `LevelBadge` in the avatar header, `DailyQuestsPanel`/`DailyQuestRow`/`DailyQuestCard` after the WeekStrip, `XpGainToast` in SceneRunner (from the new `xp:{gained,level,leveledUp}` field on the finish actions). +164 tests (704 → 868). **Season Pass deferred** to a follow-up PR (draft at `docs/superpowers/specs/2026-05-25-daily-quests-and-season-pass-design.md`). No change to existing economy/loops; migration 0022 auto-applies on deploy.

**Landmine:** *XP/quest ticks are additive + guarded + fire-and-forget.* They run AFTER an action's primary writes via `safeAwardXp` (local try/catch) + `tickQuestProgressSafe` (swallows internally) wrapped in an outer try/catch, so a tick failure logs and the action still succeeds. XP is a SEPARATE currency from coins (no conversion). Daily quests generate idempotently on home render (`generateDailyQuests`, UNIQUE `(child_id,date,quest_id)`); the chest is once-per-day via `daily_quest_chests` PK. Player level is COSMETIC only (no functional unlocks) in this PR. `bossUnlocked` quest-feasibility is approximated as "child has ever cleared a boss" (`progressRows.some(p=>p.bossCleared)`).

- **Space + Unicorn avatar themes (PR open 2026-06-06)** — two new code-only avatar themes following the PR #58 pattern: **Space / 太空** (astronaut helmet, jetpack, rocket, starfield/nebula backgrounds…) and **Unicorn & Rainbow / 独角兽彩虹** (unicorn horn, star crown, rainbow hair/skirt, star wand, rainbow-sky/pastel-cloud backgrounds…), **11 procedural-SVG items each across all 7 slots**. Registered in `AVATAR_THEMES` (`themes.ts`) — `ThemeChipStrip` maps that array so the `[太空][独角兽彩虹]` chips appear automatically; `AvatarRender` (iterates `AVATAR_SLOT_IDS`) and the seed script (7 slots + theme since PR #59) needed NO change. Catalog **41 → 63** items. No migration (`avatar_items.theme` is `text`). **Post-merge:** `pnpm tsx scripts/seed-shop-avatar-items.ts` against prod (seeds the 22 new shop rows, idempotent).

- **家 / Home module (PR open 2026-06-07)** — a decoratable home, distinct from the 🏝️ island-decoration system (PR #34). New **🏠 家 nav tab** → `/play/[childId]/home`: **3 rooms** (卧室/客厅/游戏室, `HOME_ROOMS` in `src/lib/home/rooms.tsx`, flat-SVG wall+floor backdrops on an **8×6 grid**, top 2 rows = wall zone), **~20 flat-SVG furniture items** (`src/lib/home/furniture-catalog.tsx`, 5 categories, each with `surface`/`footprint`), **tap-to-place** editor (`src/components/home/HomeRoomView.tsx` — tap tray item → valid cells highlight → tap to snap; move / 收起 put-away; view vs edit mode; optimistic local state). Furniture bought in the new **🏠 家具 shop category** (`HomeTabBody`, `'home'` added to the `shop_item_kind` enum + `purchaseShopItemInTx`'s generic group); **owned derives from `shop_purchases`** (kind `home`), **placements in `home_placements`** (migration 0023, UNIQUE `(child_id, furniture_slug)` = own-1-place-1). Server-authoritative placement validation (`src/lib/db/home.ts` `placeFurnitureInTx`: owned + in-bounds + surface-zone match + collision-free; pure grid helpers in `src/lib/home/grid.ts`). Per-child, private. +~80 tests. **Post-merge:** `pnpm tsx scripts/seed-home-furniture.ts` (already run during the build/checkpoint — migration 0023 + the 20 furniture `shop_items` are live in prod, invisible until this deploys).

**Landmine:** *Local `pnpm build` applies pending migrations to the PROD DB.* The build script is `tsx scripts/migrate.ts && next build` (PR #53) and `scripts/migrate.ts` reads `DATABASE_URL` from `.env.local` = **prod** (shared Neon). So running `pnpm build` on a feature branch applies that branch's migrations to prod early (additive ones are harmless — tables/enums sit unused until deploy — but be aware). Same spirit as the "DATABASE_URL shared across envs" landmine.

**Landmine:** *Home furniture: owned ≠ placed.* Ownership = a `shop_purchases` row (kind `home`); a placement = a `home_placements` row. The tray shows owned-but-unplaced; UNIQUE `(child_id, furniture_slug)` means each owned item is placed at most once (own-1-place-1; moving updates the row, 收起 deletes it). Rooms + furniture catalog + grid dims are CODE-only (like decor anchors / avatar catalog). Distinct from the island `decor` shop tab — don't conflate.

- **Backpack Flags-by-Continent (PR open 2026-06-07)** — World Flags pack expanded 30 → **193 UN member states**, grouped into 6 scrollable **continent** sections in the Backpack (亚洲/欧洲/非洲/北美洲/南美洲/大洋洲); each flag card tagged with its continent. Generic `PackGrouping` (optional `grouping` on `PackUiMeta` = `{resolveGroup, order, labels}`) drives a section-header branch in `PackPageBody` (extracted `PackTile` shared by flat + grouped paths); **Solar System** reuses it grouped by body `type` (`SOLAR_TYPE_ORDER` + `TYPE_EMOJI`). `continent` + `iso2` are TS fields on `FlagItem` (`flagsData.ts`); flag `emoji` is **derived from `iso2`** via `flagEmojiFromIso2` (regional-indicator transform) — never hand-typed. Disputed/non-UN territories excluded (Taiwan/Kosovo/Palestine/Western-Sahara/Vatican/…); cross-continental countries assigned one continent each (Russia→Europe, Türkiye→Asia, Egypt→Africa, …). **No DB migration, no recompile** — continent is render-derived by slug like Solar's `type`. Country data is committed static (controller-authored, not a runtime DeepSeek call — a deliberate deviation from the spec's §5). **Post-merge:** `pnpm tsx scripts/seed-flags-pack.ts` seeds the ~163 new `collectible_items` (idempotent insert-missing).

**Landmine:** *Pack grouping is client-only display config; continent is derived TS, not a DB column.* `PackGrouping` lives on `PackUiMeta` (`packRegistry.ts`) and carries a `resolveGroup` function — `PackPageBody` resolves it itself via `getPackMeta(slug)` (client-side); NEVER pass `meta`/`grouping` from a server component (same RSC hazard as `ItemCard`/`resolveRevealEmoji`). A flag's `continent` (and a solar body's `type`) is read from `flagsData.ts`/`solarSystemData.ts` by slug at render — adding/moving a country is a pure data edit + re-run of `seed-flags-pack.ts`; no migration. Flag `emoji` is derived from `iso2` via `flagEmojiFromIso2` — set `iso2`, never hand-type the glyph. Note the FlagCard continent badge renders the continent zh name, so a grouped-section test must query the header by `getByRole('heading', …)`, not `getByText`.

- **Collection & Powerup economy fixes (PR open 2026-06-07)** — three playtest fixes. **(A) Pack-complete trophy:** the `pack-complete` check only ran in the deprecated `pullPaid`; now wired into `pullCardForChild` (guarded fire-and-forget), `finishLevelAction` (surfaces the toast via `cardGrants` packSlugs), `swapShardsForItem` (guarded), and `claimWeeklyGiftIfDue`. Backfill: `scripts/backfill-pack-trophies.ts`. **(B) Universal shard wallet:** shards are now ONE global per-child wallet (`child_shards`, migration 0024), spendable on ANY pack's cards (was per-pack `shard_balances`, now dead-but-kept). Duplicates **no longer auto-convert** to shards — `pullCardInTx`/`grantGiftPackInTx` only bump the ×N count; the kid taps a `♻️ 换碎片 / To shard` chip (`convertDuplicateToShard`, 1 dupe = 1 shard, can't scrap the last copy) on owned cards with count>1. Swap stays **3 shards → any chosen unowned card** (`swapShardsInTx` reads the global wallet). Bilingual help text on the pack page explains it. Backfill: `scripts/backfill-shards-wallet.ts` (sums per-pack → global). **(C) Hint is free in practice, absent in boss:** the 💡 is a free local toggle (no token, no server call) in `PowerupTray`; hidden during the boss (`sceneSupportsHint` excludes `'boss'`). Starter pack grants skip only; `pw-hint` shop item retired (`scripts/retire-hint-powerup.ts`). +28 tests. **Post-merge ops (run after deploy):** `pnpm tsx scripts/backfill-shards-wallet.ts`, `pnpm tsx scripts/backfill-pack-trophies.ts`, `pnpm tsx scripts/retire-hint-powerup.ts`.

**Landmine:** *Shards are ONE universal wallet (`child_shards`), earned by MANUAL conversion only.* The per-pack `shard_balances` table is dead-but-kept (append-only); `getGlobalShards(childId)` is the live read, `child_shards` the live write. Card grants (boss/perfect/story/gift) NEVER auto-grant shards anymore — a dupe just bumps `child_collections.count`; `convertDuplicateInTx` (1 dupe→1 shard, requires count≥2) is the only earn path. `swapShardsInTx` spends the global wallet on any pack's card (3 = `SHARD_SWAP_COST`). `RevealCard.shardsAfter` / `GiftCard.shardsAfter` are always 0 now (kept for back-compat); the reveal shows a "重复卡 → 换碎片" note, not "+1 碎片". Don't re-point shard code at `shard_balances`.

**Landmine:** *Hint is free in practice, never in boss.* `PowerupTray`'s 💡 is a free local toggle (no `useHintAction`, no token) gated only by `hintActive` (already used this scene) + `sceneSupportsHint`. `SceneRunner` sets `sceneSupportsHint=false` for `flashcard`, `word_match`, AND `'boss'`. `BossScene` never receives a hint signal (it would reveal the answer mid-gauntlet). Skip stays a paid consumable. `grantStarterPowerupsIfNeeded` grants skip only; `pw-hint` is retired from the shop (the `'hint'` enum value + old inventory rows stay, harmless). `useHintAction` in `@/lib/actions/powerups` is now unused but kept.

- **Bilingual UI sweep (PR open 2026-06-08)** — every kid-facing chrome label is now `中文 / English` (David: kids are English-native but the point is learning Chinese, so neither language is ever dropped). New `bi(zh,en)` helper in `src/lib/i18n/bilingual.ts`. Fixed: `KidNavBar` (地图/背包/日历/家/商店 + gear all bilingual, label font → `text-[10px]`), `ShopCategoryTabs` (装扮 / Looks …), `ShopItemCard` rarity/state, `PurchaseConfirmDialog`, all shop tab bodies, `AtlasHub`/`AtlasHallCard`/`TrophiesHallCard` CTAs, `BossScene` lose-screen, scene prompts (`AudioPick`/`VisualPick`/`ImagePick`/`WordMatch`), `HomeRoomView` room+mode labels, `SwapDialog`, `WeekStrip`, `ShopHudButton`, story pages, layout title, `LevelFanfare`, `GachaPullButton`, etc. New `tests/unit/bilingual-chrome.test.tsx` regression net (fails if a nav label goes single-language). No DB change.

**Landmine:** *Bilingual chrome is the rule — `中文 / English`, ZH first.* Every NEW kid-facing label (nav, tabs, buttons, headers, dialogs, selections, scene prompts, toasts, aria-labels) must be bilingual from the start — use `bi(zh,en)` from `@/lib/i18n/bilingual` for single strings, or a ZH-span + EN-span pair in JSX. `tests/unit/bilingual-chrome.test.tsx` guards the nav. **Exempt:** collectible card names (already bilingual), AI-generated content (story chapters / scene stimuli text), the zodiac character glyphs themselves (那是 the content being learned), and the parent `/parent` admin (David-facing). Many existing labels were already bilingual via `xxxZh`+`xxxEn` prop pairs (`SectionCard`, `TrophiesBody`, `SceneRunner` segments) — leave those.

- **Avatar try-on shop (PR open 2026-06-08)** — Yinuo can try cosmetics before buying. A **big `AvatarRender` (size 200)** sits atop the avatar shop tab (`AvatarTryOnPreview`). Tapping ANY grid item: **owned → equips** (optimistic) + clears try-on; **unowned → try-on** (`tryOn` state in `ShopBody`, no server call, free even when unaffordable) — the big avatar shows the equipped look with the tried slot overridden, and a contextual **购买 / Buy · 🪙price** bar appears (`onBuy` → `purchaseShopItemAction` then `equipAvatarItemAction` then clear). `卸下 / Take off` reverts; switching category tabs discards `tryOn`. The tried card gets a `试穿中 / Trying` badge (`ShopGrid` `tryingShopItemId` → `ShopItemCard` `trying`). **The avatar `PurchaseConfirmDialog` is retired** (the Buy bar replaces it) — the component file + its own tests stay for reference; other shop kinds were never wired to it. No DB change.

**Landmine:** *Avatar purchase goes through the try-on Buy bar, not `PurchaseConfirmDialog`.* `ShopBody`'s avatar tab no longer renders `PurchaseConfirmDialog`; `handleTryOn` (unowned tap) sets ephemeral `tryOn`, and `handleBuyTryOn` does purchase→equip→clear. Try-on is client-only state (never persisted). If you re-add a confirm step, thread it through `AvatarTryOnPreview`'s Buy bar — don't resurrect the dialog for avatars.

- **Backpack card-detail view + World Landmarks pack (PR open 2026-06-09)** — two backpack follow-ups. **(A) Tap-to-open card detail:** new `CardDetailDialog` (`src/components/play/CardDetailDialog.tsx`) — generic across ALL packs by rendering the pack's own `meta.ItemCard` at `size="lg"` (which already shows capital/location/type + lore when owned) + a `SpeakButton` 🔊 read-aloud of the ZH name; unowned shows a locked teaser. `PackPageBody` PackTile is now a tap target (`data-testid="card-tap"`, role=button) → opens it; the swap/convert chips `e.stopPropagation()` so they still work. Resolves `meta` client-side from `packSlug` (RSC-safe). **(B) New `landmarks-v1` pack** — 世界地标 / World Landmarks, **17 landmarks** grouped by the SAME 6 continents as flags (reuses `Continent`/`CONTINENT_LABELS`/`CONTINENT_ORDER` from flagsData). New `landmarksData.ts` + `LandmarkCard.tsx` (mirrors FlagCard: emoji, bilingual name, 📍location line, continent badge, lore at lg) + `packRegistry` entry (grouping by continent) + `scripts/seed-landmarks-pack.ts`. Emoji glyphs (limited for landmarks — differentiates by name+location, like dinosaurs). No DB migration. **Post-merge:** `pnpm tsx scripts/seed-landmarks-pack.ts` (a 6th active pack → the weekly 大礼包 now grants 6 cards, one per pack, by the derived-not-hardcoded rule).

- **Collectible card art — `<CardArt>` rendering + Codex backfill handoff (PR open 2026-06-09)** — the 62 weak-emoji cards (sea-creatures 20 / dinosaurs 15 / solar-system 10 / landmarks 17) get **real cartoon art**. **(Code, this PR):** new shared `src/components/play/items/CardArt.tsx` renders `<img>` when `collectible_items.image_url` is a real `http(s)` URL, else the emoji glyph (img-with-emoji-fallback, mirrors `ImageWordScene`; unowned = `opacity-40 grayscale` in both modes). Wired into `SeaCreatureCard`/`DinosaurCard`/`SolarBodyCard`/`LandmarkCard` (the per-card `emojiSize` map + the `?? item.imageUrl` emoji-fallback middle were removed — `image_url` is now a real-image channel, NOT an emoji store; the emoji lives only in the TS data files via `meta.emoji`). Flags (real flag emoji) + zodiac (procedural SVG) deliberately NOT wired. **(Art — DONE 2026-06-09 via Cloudflare):** all 62 images generated with **Cloudflare Workers AI** (`@cf/black-forest-labs/flux-1-schnell`, 1024² JPEG) by `scripts/generate-collectible-art-cloudflare.ts` (CF creds from `CF_ACCOUNT_ID`/`CF_API_TOKEN` env — never committed; idempotent; per-pack prompt recipe + per-planet traits + per-slug NSFW-false-positive overrides for seashell/dolphin), uploaded to Blob at `collectibles/{itemId}.jpg`, written to `collectible_items.image_url`. `scripts/verify-collectible-images.ts` confirms 62/62, all HTTP-200. `docs/codex-collectible-art-backfill.md` retained as fallback/reference. No migration, no recompile — `image_url` is read at request time. +4 tests (`tests/unit/card-art.test.tsx`). **The live cards show this art once PR #76 (the `<CardArt>` rendering) deploys** — the DB already holds the URLs.

- **Landscape 16:9 shell + key surfaces (PR open 2026-06-10)** — first of a 3-PR arc David requested (16:9 shell → home map art → lunar calendar + Monthly Challenge). The app is played on a landscape iPad but was a narrow centered phone column (`max-w-md` + bottom `KidNavBar` + `pb-20`). This PR makes it landscape-native at the **`lg:` (≥1024px) breakpoint, pure CSS, no JS orientation probe** (phones/portrait unchanged): (1) `KidNavBar` is now **ONE nav that morphs** — sticky bottom bar on phones, `lg:fixed left rail` (`lg:w-20 lg:flex-col`, gear `lg:mt-auto`); single DOM tree so links aren't duplicated. (2) `play/layout.tsx` swaps `pb-20 → lg:pb-0 lg:pl-20` (header `lg:pl-24`) to clear the rail. (3) Home `page.tsx` → two-pane at `lg:` (`lg:grid lg:grid-cols-[minmax(300px,360px)_1fr]`): HUD column left (avatar/coins/weekstrip/quests/chapter) · map pane right; stacked on phones (pure JSX wrapping, no data change). (4) Scene split lives in **one shared component** — `MultipleChoiceQuiz` becomes `lg:grid lg:grid-cols-2` (prompt+stimulus left, choices right, `lg:max-w-lg`), lighting up AudioPick/ImagePick/ImageWord/TranslatePick/SentenceCloze at once. Standalone scenes (Flashcard/Lianliankan/Boss) already center+fill (no `max-w-md` cap) so untouched. Spec: `docs/superpowers/specs/2026-06-10-landscape-16-9-shell-design.md`. No DB migration, no recompile, no new deps. +2 tests (KidNavBar morph, MCQ split). **Landmine:** the landscape switch is the `lg:` width breakpoint convention — new kid-facing surfaces should add `lg:` layout (rail is reserved via `lg:pl-20` on the layout content div; don't re-pad per-page).

- **Home map art — landscape-fit illustrated voyage board (#78, 2026-06-10)** — PR-2 of the 3-PR arc (16:9 shell #77 → **home map art** → calendar). The home voyage board was a tall vertical-scroll CSS-parchment zigzag; in the new landscape pane that felt wrong. Now: a **landscape-fit map (no scroll) on `lg:`**, phones keep the vertical scroll board. New `voyageLayoutHorizontal(total)` (boustrophedon snake — 1 row ≤5 stops, else 2 rows top L→R / bottom R→L) alongside the unchanged vertical `voyageLayout`. Layout chosen at runtime by new `useIsWide()` hook (`src/lib/hooks/use-is-wide.ts`, `useSyncExternalStore` over `matchMedia('(min-width:1024px)')` — SSR snapshot `false`, same blessed pattern as the Web-Speech hooks; **no DOM duplication**, one board rendered). New `VoyageBackdrop` (`<img object-cover>` when a flux backdrop exists, else a **richer procedural sea-chart** — sea+waves+compass-rose+islands+sea-monster SVG) and `SailingShip` (framer-motion ship that sails the route on a loop; reduced-motion → parked static at the current stop). Shared textures extracted to `src/lib/play/voyage-textures.ts`. `VoyageMap` gained **optional** `imageUrl?` (`map-boards.ts`, client-only config — still no DB). **The flux backdrop images are NOT generated yet** (no CF token at build): the board ships with the procedural backdrop and swaps to the illustration the moment the 2 URLs are pasted into `VOYAGE_MAPS[...].imageUrl`. Generator ready: `scripts/generate-voyage-map-art.ts` (needs a fresh `CF_API_TOKEN`; ~2 flux gens, prints URLs to paste). +13 tests. No DB migration, no recompile, no new deps (framer-motion already present).

**Landmine:** *Home map backdrop is optional + degrades to procedural; layout switches via `useIsWide()` (a hook, not CSS).* `VoyageBoard` renders ONE board whose layout (`voyageLayout` vertical vs `voyageLayoutHorizontal` landscape) and container (tall-scroll vs `aspect-[16/10]` no-scroll) are chosen by `useIsWide()` — jsdom has no `matchMedia` so the hook returns `false` (vertical) in tests; mock `@/lib/hooks/use-is-wide` to test the landscape path. The flux illustration is keyed off `VoyageMap.imageUrl` (client-only config in `map-boards.ts`, NOT a DB column) — absent → `VoyageBackdrop` draws the procedural sea-chart. `SailingShip` + medallions share the `pos` coordinate space (full-board %); the dotted route SVG is inset 30px (decorative, slight offset by design, same as pre-PR). **Post-merge op (David's CF token):** `CF_ACCOUNT_ID=… CF_API_TOKEN=… pnpm tsx scripts/generate-voyage-map-art.ts` → paste the 2 URLs into `map-boards.ts`.

- **Lunar calendar + festival Monthly Challenge (#79, 2026-06-10, Phase A)** — PR-3 of the arc. `/calendar` now shows the **农历 calendar**: each cell carries the lunar day (初一/十五…) + a festival/24节气 badge, computed **server-side** via new `src/lib/calendar/lunar.ts` (`lunarInfo(iso)`, wrapping the new `lunar-typescript` dep — kept off the client bundle by passing plain `{dayZh,emoji,label}` into the client grid). A **festival Monthly Challenge** sits atop: a fixed Gregorian-month → festival-theme map (`src/lib/calendar/festivals.ts`, 12 themes — 春节🧧/端午🐲/中秋🌕/冬至🥟/立夏🍃…; fixed-not-农历-accurate so the goal is predictable, while the GRID badges show real 农历 dates) + an **active-days progress bar** (`FestivalChallengePanel`); at `thresholdDays` (12) active days the kid claims a **festival collectible card** (`CardChestReveal`). New `festivals-v1` collectible pack (12 emoji cards, `festivalsData.ts` + `FestivalCard` + packRegistry entry) — **reward-only**: shown in Backpack (`is_active=true`) but excluded from gacha/大礼包 via a new additive `collection_packs.gacha_eligible` flag (festivals=false; both `pullCardInTx` + `grantGiftPackInTx` filter on it). Engine: `src/lib/db/festival-challenge.ts` (`getMonthlyChallengeState` read + `claimFestivalReward` tx, idempotent per `(child, month)` via new `festival_challenge_claims` PK). Action `claimFestivalRewardAction`. Migration **0025** (gacha_eligible + festival_challenge_claims). Spec: `docs/superpowers/specs/2026-06-10-lunar-calendar-festival-challenge-design.md`. +9 tests. **Phase B (PR-3b, fast-follow):** festival avatar cosmetics as a second reward (David asked for both card + cosmetics; the card is Phase A). **Post-merge op:** `pnpm tsx scripts/seed-festivals-pack.ts` (seeds the pack `gacha_eligible=false` + 12 cards — REQUIRED before any claim can grant a card).

**Landmine:** *Festival cards are reward-only — `gacha_eligible=false`.* The `festivals-v1` pack is `is_active=true` (visible in Backpack) but `gacha_eligible=false` so its cards NEVER drop from boss/perfect/story pulls or the weekly 大礼包. Both gacha paths (`pullCardInTx` catalog, `grantGiftPackInTx` per-pack loop) filter `and(isActive, gachaEligible)`. The 大礼包 grants "one card per active **gacha-eligible** pack" — adding a future reward-only pack won't inflate the gift. The monthly challenge theme is a FIXED Gregorian-month map (`festivals.ts`); never map a fixed month to an exact festival date (农历 drifts) — the calendar grid badges use `lunarInfo` for real dates. `lunar-typescript` must stay server-side (compute in the calendar page, pass plain data to `MonthCalendar`). `claimFestivalReward` throws if the pack isn't seeded — run `seed-festivals-pack.ts` post-merge.

- **Festival avatar cosmetics (PR open 2026-06-10, Phase B of PR-3)** — the 2nd monthly-challenge reward. **12 reward-only festival avatar cosmetics** (one per month theme: 派对帽/春节喜帽/元宵灯笼/柳枝环/夏叶/龙舟头饰/夏至骄阳/七夕喜鹊/中秋兔耳/重阳菊花/立冬暖帽/冬至饺子), procedural-SVG in `itemCatalog.tsx` (slots hat/decor, theme `'festival'`, new `rewardOnly: true` flag → excluded from `defaultItems()` + shop; new `rewardItems()`). New `'festival'` theme in `AVATAR_THEMES` + `THEME_DISPLAY_NAMES`; `SHOP_FILTER_THEMES` (= themes minus festival) drives `ThemeChipStrip` so festival items never show as a shop chip. `festivals.ts` themes gained `avatarItemRef`. `claimFestivalReward` now ALSO grants + **auto-equips** the month's cosmetic (`grantFestivalCosmeticInTx`: inventory `onConflictDoNothing` + equipped `onConflictDoUpdate`; best-effort — returns `cosmetic: null` if the avatar item isn't seeded, never fails the card grant) and returns `cosmetic`. `FestivalChallengePanel` shows a "🎀 新装扮" banner with a mini `AvatarRender` wearing it. Action revalidates home + shop. **No migration** (avatar_items + existing slots; `theme` is text). +17 tests. **Deferred follow-up:** a festival *wardrobe* to re-equip past cosmetics (right now they're auto-equipped on claim + live in inventory, but the shop tab only lists purchasable items, so swapping back to an old festival cosmetic has no UI yet). **Post-merge op:** `pnpm tsx scripts/seed-festival-avatar-items.ts` (REQUIRED — seeds the 12 `avatar_items` rows; until then `cosmetic` is null and only the card is granted).

**Landmine:** *Festival avatar cosmetics are reward-only (`rewardOnly` ItemDef flag, `unlock_via='achievement'`).* They're NOT in `shopItemsCatalog()`/`defaultItems()` and NOT in the shop `ThemeChipStrip` (`SHOP_FILTER_THEMES` excludes `'festival'`). `equipAvatarItem` gates on `child_avatar_inventory` (not shop_items), so a granted festival item IS equippable — it's just only auto-equipped on claim today (no wardrobe UI yet; deferred). `claimFestivalReward` grants the cosmetic best-effort (`grantFestivalCosmeticInTx` returns null if `seed-festival-avatar-items.ts` hasn't run) — the card grant never depends on it. Adding a new avatar theme that's reward-only: set `rewardOnly` + add to `AVATAR_THEMES` but keep it out of `SHOP_FILTER_THEMES`.

- **Home polish 2.5D + swappable wallpaper/flooring (PR open 2026-06-11)** — the 家 rooms get a "modern game" art lift + a decoration expansion (David chose this over a full isometric rework, from a rendered 4-way mockup). **Depth system:** soft contact shadow under every placed item (`PlacedFurniture`), plus a `RoomCanvas` overlay — cove shadow (ceiling), 3D skirting board, warm floor light-pool — all `useId`-scoped gradients. **Swappable surfaces:** new `src/lib/home/surfaces.ts` catalog (7 wallpapers + 7 floors; 3 room defaults free + 4 buyable each), `home_room_surfaces` table (migration 0026, PK child+room), `src/lib/db/home-surfaces.ts` (`getRoomSurfaces` defaults-applied + `setRoomSurface` ownership-checked), `setRoomSurfaceAction`. `RoomCanvas` now renders the equipped wallpaper+floor (`RoomDef.Backdrop` kept but unused/back-compat) — defaults are the *polished* versions of the old backdrops. Shop `HomeTabBody` gains 墙纸/地板 sections; `SurfacePicker` in the home editor swaps per-room (owned + free defaults, optimistic). Surfaces sell as `kind='home'` (reuse the generic purchase path; defaults equippable without owning). `home_room_surfaces` absent row → room default. **All 20 furniture sprites repainted** with a consistent top-left light (added highlight + under-shadow tones + rounder corners, geometry/footprints unchanged) in `furniture-catalog.tsx`. +13 tests. **Post-merge op:** `pnpm tsx scripts/seed-home-surfaces.ts` (seeds the ~8 buyable surface `shop_items`; migration 0026 auto-applies on deploy).

**Landmine:** *Home surfaces are `kind='home'` shop_items + a per-room equipped table; defaults are free.* Buyable wallpapers/floors seed as `shop_items(kind='home')` (same generic purchase path as furniture — `getOwnedFurnitureSlugs` returns BOTH; filter by `getSurface(slug)` to get surface slugs). Equipped state lives in `home_room_surfaces` (absent → `ROOM_DEFAULT_SURFACES`); `setRoomSurface` allows a slug that `isDefault` OR owned (never require buying a default). Surface `render()` fills ONLY its zone (wallpaper y0..25, floor y25..75) with slug-scoped gradient ids (unique per page). The `RoomCanvas` depth overlay uses `useId`-scoped ids — don't hardcode gradient ids there (two rooms/shop swatches collide).

- **Home yard room + room-art polish + bigger canvas (PR #84, merged 2026-06-11)** — follow-up to the home-polish PR (David's playtest: furniture got repainted but rooms still read flat/small + wanted an outdoor space). **New 4th room 院子 / Yard (outdoor):** reuses the surface system — sky = `wallpaper`-kind (`sky-day` default + `sky-sunset`), ground = `floor`-kind (`ground-lawn` default + `ground-sand`/`ground-deck`); `ROOM_DEFAULT_SURFACES.yard` + `validCells` (surface=`floor`) keep furniture on the ground, sky clear. **5 outdoor furniture** (`yard-swing` 2×2 / `yard-sandbox` / `yard-picnic-table` / `yard-tree` 1×2 / `yard-flower-bed`) mapped onto existing `furniture`/`plant_toy` categories (no enum change), sold in the `home` shop category. **Room-art polish:** new `grad3` 3-stop gradients + richer texture on the 3 indoor default surfaces; stronger **generic** `RoomCanvas` depth overlay (corner AO vignette + top-left window-light wash + clearer horizon) — one place, all rooms. **Bigger canvas:** home page `max-w-md → max-w-2xl lg:max-w-4xl` (grid stays 8×6). `RoomDef` gained an `emoji` field; `RoomTabs`/`HomeRoomView` read it (killed the hardcoded 3-room ternaries) so a 4th tab appears automatically. **No migration, no recompile.** Post-merge seeds RAN: `seed-home-furniture.ts` (+5) + `seed-home-surfaces.ts` (+3) — live in prod. +17 tests. **Landmine:** *adding a home room = add to `HOME_ROOMS` + `ROOM_DEFAULT_SURFACES` (and give it `emoji`); everything else (tabs, surfaces, grid zones, getRoomSurfaces) keys off those two so it wires automatically.* The shared surface catalog means a sky wallpaper also shows as an option in the indoor rooms' `SurfacePicker` (whimsical, allowed by design).

- **Festival wardrobe (PR #86, merged 2026-06-12)** — closes the deferred PR-3b follow-up. Festival avatar cosmetics are reward-only (`theme='festival'`, `unlock_via='achievement'`, never in the shop grid) so once the kid swapped to a normal look there was no way back. New **🎀 节日衣橱 / Festival Wardrobe** strip at the bottom of the avatar shop tab: `listOwnedFestivalCosmetics(childId)` (joins `child_avatar_inventory`→`avatar_items` theme='festival' + left-joins `child_avatar_equipped` for the worn flag); `FestivalWardrobe` renders a mini `AvatarRender` per owned cosmetic with a bilingual name from `FESTIVAL_THEMES` (avatarItemRef→nameZh/nameEn), ringed when worn, **hidden when nothing unlocked**. `ShopBody` equips optimistically via `equipAvatarItemAction` (gates on inventory, so reward items ARE equippable), clearing same-slot peers. No migration/seed. +4 tests.

- **Card-grant rebalance (PR open 2026-06-12)** — David: completing a section should earn cards, fairly. **Review** completion → 1 card **once per (week, day)** (`refId = ${weekId}:${todayUtcIso()}`); a same-day repeat of the SAME week's review → fanfare notice "今日回顾已完成,不再获得新卡片"; a DIFFERENT week earns again. **Practice** completion → 1 card every full completion, **cap-only** (`refId = sessionId`; the grind path). **Boss** unchanged. Any source hitting the shared **daily cap (10)** → "今日卡片已经发放完毕,明日再来". `pullCardInTx`/`CardGrantSource` gained `'review'|'practice'`; `finishLevelAction` gained `section` (FinishLevelSchema, default `'boss'`) + a `pullSectionCard` helper returning `{card, skip}` + a new `cardMessage` return field; `SceneRunner` threads `section` (from the section page) and surfaces `cardMessage` into `LevelFanfare`. Review/practice cards join `cardGrants` so the `earn_card` quest tick + `pack-complete` trophy loop cover them automatically. No migration, no recompile. +~9 tests (spec: `docs/superpowers/specs/2026-06-12-card-grant-rebalance-design.md`).

**Landmine:** *`finishLevelAction` boss detection is now SECTION-based, not last-level-based.* `bossCleared = parsed.section === 'boss' && allScenesCleared`. Before this PR it used the week's last level (always a boss) + section-relative "all passed", so finishing **review/practice** on a ≥10-char week **mis-fired boss-clear** (paid 300 coins, granted a boss card, marked the week cleared). The `section` param (`'review'|'practice'|'boss'`, default `'boss'`) is threaded section-page → `SceneRunner` → action and is REQUIRED for correct rewards. `upsertWeekProgress` only ever upgrades `bossCleared` to `true`, so review/practice passing `false` never un-clears a real boss. Tests that simulate "non-boss completion" must pass `section:'practice'` (NOT rely on a non-boss last scene type).

- **Backpack continent rewards + nav — PR-A (PR open 2026-06-12)** — completing a whole continent's flags now rewards the kid. David's decisions: reward = **trophy + continent avatar cosmetic**, nav = **scroll-jump**, scope = **flags only**. **Split for review:** PR-A = nav + completion **trophies** (this PR); **PR-B** = the 6 continent avatar cosmetics + wardrobe (follow-up; Yinuo hasn't completed any 193-flag continent so no backfill needed). New `src/lib/collections/continentRewards.ts` (pure: `CONTINENT_REWARDS` continent→`{trophySlug,avatarItemRef}` + reverse `TROPHY_TO_CONTINENT`; `avatarItemRef` unused until PR-B). 6 trophies `continent-asia`…`continent-oceania` in `seed-trophies.ts` (collection cat). `getCompletedFlagContinents(childId)` in `trophies-evaluators.ts` (groups `flags-v1` catalog by `FLAGS_BY_SLUG[slug].continent`, returns fully-owned continents). `checkAndGrantTrophies` gained a `{kind:'continent-complete'}` case. **Nav:** generic scroll-jump chip strip in `PackPageBody` (one chip per non-empty `meta.grouping` group — benefits flags + landmarks + solar; sections got `id="pack-section-<key>"` + `scroll-mt-14`). **Grant wiring (idempotent, guarded):** `finishLevelAction` surfaces continent trophies via the existing `TrophyToast` when a granted card is `flags-v1`; `swapShardsForItem` now returns `continentTrophies` (surfaced by `PackPageBody`'s `TrophyToast` — the deliberate swap-for-last-flag completion); `pullCardForChild` + `claimWeeklyGiftIfDue` do a guarded fire-and-forget grant so story/gift completions never miss it. +7 tests. No migration, no recompile. **Post-merge op:** `pnpm tsx scripts/seed-trophies.ts` (idempotent — inserts the 6 new trophies).

**Landmine:** *Trophies are DB rows seeded by `scripts/seed-trophies.ts` (NOT by `migrate.ts`).* Adding a trophy = add a row to `TROPHIES` in that script + (usually) a new `checkAndGrantTrophies` case/evaluator, then run the seed against prod post-merge (idempotent: skips existing by slug). Tests mock `@/db`, so a missing seed run only surfaces in prod (a granted slug with no `trophies` row silently grants nothing — `checkAndGrantTrophies` filters to existing rows). Continent completion is grant-path-wired in FOUR places (finishLevelAction / swapShardsForItem / pullCardForChild / claimWeeklyGiftIfDue) — all idempotent via the trophy dedup, so multiple paths checking is safe; only newly-earned trophies are ever returned/surfaced.

- **Backpack continent cosmetics — PR-B (PR open 2026-06-12)** — the deferred half of the continent-rewards arc. 6 reward-only avatar cosmetics (`theme='continent'`, slot `hat`): 亚洲斗笠 / 欧洲皇冠 / 非洲探险帽 / 北美牛仔帽 / 南美羽冠 / 大洋洲花冠 (procedural SVG in `itemCatalog.tsx`, `rewardOnly:true`). New `'continent'` theme in `AVATAR_THEMES`/`THEME_DISPLAY_NAMES`; `SHOP_FILTER_THEMES` now excludes BOTH reward themes via a `REWARD_THEMES` list (festival + continent) so neither shows as a shop chip. **Granted + auto-equipped** alongside the continent trophy: new `src/lib/db/continent-rewards.ts` `grantContinentRewards(childId)` (calls `checkAndGrantTrophies('continent-complete')` then grants+equips the cosmetic for each NEWLY-earned continent via `TROPHY_TO_CONTINENT`→`avatarItemRef`, mirroring `grantFestivalCosmeticInTx`); it REPLACED the bare `checkAndGrantTrophies('continent-complete')` calls in all 4 PR-A grant sites (finishLevelAction / swapShardsForItem / pullCardForChild / claimWeeklyGiftIfDue). **Wardrobe generalized:** the festival wardrobe became the **奖励衣橱 / Rewards Wardrobe** — `listOwnedFestivalCosmetics`→`listOwnedRewardCosmetics` (theme IN festival,continent), `FestivalWardrobe`→`RewardWardrobe` (label map merges `FESTIVAL_THEMES` + `CONTINENT_LABELS`), `FestivalCosmeticListing`→`RewardCosmeticListing` (added `theme`), ShopBody prop `festivalCosmetics`→`rewardCosmetics`. +9 tests. No migration, no recompile. **Post-merge op:** `pnpm tsx scripts/seed-festival-avatar-items.ts` (now seeds ALL `rewardItems()` = 12 festival + 6 continent; idempotent, inserts the 6 new).

**Landmine:** *`grantContinentRewards` (NOT bare `checkAndGrantTrophies`) is the continent grant entrypoint now.* It grants the trophy AND auto-equips the reward cosmetic; all 4 grant sites call it. Tests that exercise `finishLevelAction`/`gacha` must `vi.mock('@/lib/db/continent-rewards', () => ({ grantContinentRewards: vi.fn().mockResolvedValue([]) }))` or they pull in real `@/db` (DATABASE_URL error) — same hazard as any new `@/lib/db/*` import into an action under test. Reward-only avatar themes are listed in `REWARD_THEMES` in `themes.ts` (festival + continent) — add a new earned theme there to keep it out of the shop chip filter AND broaden `REWARD_WARDROBE_THEMES` in `shop.ts` to surface it in the wardrobe.

- **Pirate Homework (PR #90, merged 2026-06-13)** — David's top-priority feature. Parent-authored weekly homework as a **new per-week section** alongside 回顾/练习/Boss战. **3 exercise types** (manually authored, NO AI): `char_quiz` (David's Chinese question + 2–4 bilingual `{textZh,textEn}` options + correctIndex), `word_building` (baseChar + correctWord + distractors → MCQ "给「X」组词"), `sentence_order` (tap word-blocks into order — `SentenceOrderScene`). **Standalone subsystem — NOT compiled into `week_levels`** (David edits homework anytime, no recompile). New `homework_items` table (migration 0027: table + `homework_item_type` enum + `coin_reason 'homework_complete'` value) storing a zod-validated jsonb `config` per item (`src/lib/homework/schemas.ts` `parseHomeworkConfig`). DB layer `src/lib/db/homework.ts` (CRUD + reorder + `weekHasHomework`/`listHomeworkItems`). Parent authoring: `src/lib/actions/homework.ts` actions + `HomeworkEditor` (+ 3 per-type forms) mounted on `/parent/week/[id]/review` (parent-facing → bilingual rule exempt). Kid play: `/play/[childId]/homework/[weekId]` → `HomeworkRunner` (reuses `MultipleChoiceQuiz` for char_quiz/word_building, `SentenceOrderScene` for sentence_order; choices shuffled UPSTREAM). Reward via `finishHomeworkAction`: coins (`homework_complete`) + XP (`'homework'` XpSource) + **1 card, once per (week, day)** via a new `'homework'` card source (`refId=${weekId}:${dayUtc}`), ALL gated on the card pull's `granted` path (can't farm); repeat-same-day → fanfare "今日作业已完成". WeekHub shows a conditional 📚 作业 card (only when `listHomeworkItems` non-empty). Built via subagent-driven-development (13 tasks, TDD). +27 tests. No seed, no recompile, no post-merge ops.

**Landmine:** *Homework is a standalone subsystem; its reward path mirrors the section-card mechanic.* `finishHomeworkAction` awards coins+XP+card ONLY on the `pullCardForChild('homework', ...)` `granted` branch (the card-grant log is the single idempotency source — `awardCoins` itself isn't idempotent, but the duplicate pull returns `already_granted` so the second award can't fire). `'homework'` is a card source in BOTH `CardGrantSource` (gacha.ts) AND the `pullCardInTx` source union (grants.ts); `'homework_complete'` is in BOTH the `coinReason` pgEnum (economy.ts) AND `AwardCoinReason` (coins.ts). Homework config is jsonb validated by `parseHomeworkConfig(type, raw)` (throws ZodError) — the kid route + summaries parse defensively (skip invalid). Homework does NOT gate boss-unlock (boss reads `practice` only). The `HomeworkRunner` shuffles MCQ choices upstream (MCQ-randomness landmine); `SentenceOrderScene` uses a seeded Fisher-Yates + anti-identity guard (a plain `(a*k)%n` comparator collapses to identity for n=3/n=7 — the common sentence lengths).

- **Distribution prep: per-child homework + parent simplify + story hidden + name privacy (PRs #92/#93/#94, merged 2026-06-13)** — the product is now distributed to DIFFERENT kids' accounts, so several single-family assumptions were fixed. **#92 (hotfix):** the parent PIN gate caused an infinite `/parent/unlock` redirect loop once the 15-min cookie expired (the gate lived in the shared `/parent` layout, which also wraps `/parent/unlock`). Fixed by a **`(secured)` route group** — `src/app/parent/(secured)/layout.tsx` does the PIN gate; the base `src/app/parent/layout.tsx` does auth-only; `/parent/unlock` is OUTSIDE the group. URLs unchanged. **#93:** Story Mode HIDDEN (DeepSeek ZH quality not up to par) — `LatestChapterPill` / Story-Library-card / boss→story-exit removed, eager gen in `finishLevelAction` disabled, both story routes guarded by a `const STORY_HIDDEN = true` → `notFound()` (flip to re-enable; code + `story_chapters` retained). Hardcoded **"Yinuo"** removed from user-visible surfaces (parent PIN banner, add-child placeholder, story loading, AtlasHub). **#94:** **homework is now PER-CHILD** — `homework_items` gained `child_id` (migration 0028; FK to `child_profiles`, index `(child_id, week_id)`) and EVERY read/write is scoped by `(childId, weekId)` so one account's homework never shows to another kid on the shared pack week. Parent admin simplified to **manage children + add homework**: AI week-generation hidden (dashboard buttons + Recent-custom-weeks removed; `/parent/week/new`, `/parent/stage/new`, `.../review` routes kept but unlinked). New per-child homework flow: child page (`/parent/children/[id]`) lists the child's playable weeks → `/parent/children/[id]/homework/[weekId]` → `HomeworkEditor` (childId+weekId threaded through editor + 3 forms + all actions, which assert `getChildOwnedBy` + `getPlayableWeekForChild`).

**Landmine:** *Parent PIN gate lives in the `(secured)` route group, NOT the base `/parent` layout.* Putting the `parentPinHash && !unlocked → redirect('/parent/unlock')` check in a layout that ALSO wraps `/parent/unlock` creates an infinite redirect loop the moment the cookie expires. Keep gated pages under `src/app/parent/(secured)/`; keep `/parent/unlock` directly under `src/app/parent/` (base layout = auth-only). *Homework is PER-CHILD (`homework_items.child_id`)* — never query/mutate homework by `weekId` alone; always scope by `(childId, weekId)`, else one kid's homework leaks to another account on a shared pack week. *Never hardcode a child's name in user-visible UI* (the product is distributed) — names come from `child_profiles.displayName`; use generic copy/placeholders. *Story mode is hidden behind `STORY_HIDDEN` flags* (story page + story-library page) plus removed entry points; to re-enable, flip the flags AND restore the entry points + eager gen.

- **Season Pass (PR #96, open 2026-06-13)** — the deferred PR C-2. A calendar-fixed, limited-time **30-tier reward track** ("夏季航海 / Summer Voyage") built on the shipped XP currency, with **zero changes to any existing earn loop**. New `seasons` (typed `tier_config` JSONB) + `child_season_progress` (`tiers_claimed int[]`) tables (migration 0029; also adds `coin_reason 'season_reward'` + `trophy_category 'season'`). **Season XP is DERIVED** = `SUM(xp_events.amount)` within the season's `[starts_at, ends_at]` window (`getSeasonXp`) — `child_xp.totalXp` / player level are never touched, no "reset." 30 tiers dispatch by reward type (`SeasonReward` union in `src/lib/season/types.ts`; config in `src/lib/season/summerVoyage.ts`): coins / powerup / shards / **card** (specific card from the reward-only `season-summer-v1` pack — `gacha_eligible=false`, like festivals) / **cosmetic** (reward-only `theme:'season'` avatar item, granted to inventory, NOT auto-equipped) / **cosmetic_set** (T30 grand 船长大衣+帽, **auto-equipped** + `season-summer-master` trophy). `claimSeasonTierInTx` (`src/lib/db/season.ts`) does the per-type grant + appends the tier (idempotent via the `int[]`). **Claim model:** per-tap claim + `CardChestReveal` during the season; **end-of-season auto-bank — nothing lost** (`syncSeasonProgress` sweeps reached-but-unclaimed tiers ONLY after `ends_at`, called on home render). UI: ⛵ `SeasonBanner` below Daily Quests on home → `/play/[childId]/season` (`SeasonTrack`, bilingual, `lg:` landscape, reduced-motion safe). Actions `claimSeasonTierAction` / `claimAllSeasonTiersAction`. 8 season cosmetics added to `itemCatalog` (`rewardItems()` auto-includes them → `seed-festival-avatar-items.ts` seeds them with no script change); `'season'` added to `AVATAR_THEMES` + `REWARD_THEMES` + `REWARD_WARDROBE_THEMES`. CF-flux recipe for the 4 cards added to `generate-collectible-art-cloudflare.ts`. +~25 tests. **Decisions (David):** calendar-fixed 8-week live-now season; auto-bank no-FOMO; 1.5× final-week multiplier dropped for v1 (fights the derived-XP model). **Post-merge ops (run IN ORDER):** `seed-season-cards.ts` → `seed-festival-avatar-items.ts` → `seed-trophies.ts` → `seed-season-summer.ts` (the season row LAST; tiers reference the above).

**Landmine:** *Season XP is derived from the `xp_events` ledger, never stored — don't add a season-XP column or reset logic.* `getSeasonXp` sums `xp_events.amount` in the `[starts_at, ends_at]` window; a new season is just a later window. `child_xp.totalXp`/level stay lifetime-permanent. Consequence: the dropped 1.5× final-week multiplier can't be retroactively applied by re-summing — if re-added, emit a separate `season_bonus` XP event at award time. *Season cards/cosmetics are reward-only* (`season-summer-v1` pack `gacha_eligible=false`; `theme:'season'` in `REWARD_THEMES`) — never dropped by gacha/大礼包, never shown as a shop chip; surfaced in Backpack + the 奖励衣橱. *Claim is per-tap during the season; the only auto-grant is the end-of-season sweep* (`syncSeasonProgress`, gated on `now > ends_at`) — don't make home render auto-claim mid-season (it'd skip the reveal). *`claimSeasonTierInTx` grants cosmetics to inventory WITHOUT equipping* (except the T30 set) — a 30-tier track auto-equipping each cosmetic would thrash the kid's look. Tests that import `scripts/seed-trophies.ts` rely on its auto-run being guarded by `import.meta.url === pathToFileURL(process.argv[1]).href` (so `{ TROPHIES }` is importable without running `main()`/`process.exit`).

- **Avatar gender + ears (PR #97, merged 2026-06-13)** — two avatar asks from David. **(1)** Boy/girl distinction chosen **at child creation**: new `child_profiles.gender` column (migration 0030, nullable text `'boy'|'girl'`, null=neutral); `AddChildForm` 男孩/女孩 radio → `createChildAction` (zod `gender` field, nullish) → `createChildProfile`. **(2)** Ears on every head SVG. The live mechanism: `getEquippedAvatar` resolves the **head**-slot default by the child's gender via `defaultHeadForGender` (`src/lib/avatar/defaultLook.ts`: `GENDER_DEFAULT_HEAD` boy→`default-kid-boy`, girl→`default-kid-girl`, else neutral `default-kid-warm`) — flows through `equippedRefs` → `AvatarRender` everywhere (home HUD, shop preview, reveals). Two new head ItemDefs (`default-kid-boy` thick brows / `default-kid-girl` eyelashes+blush+lips), both **free defaults** (so a kid can also swap them in the avatar shop); ears added to warm/caribbean/space/unicorn heads too. Gendered heads are `defaultItems()` → `seed-shop-avatar-items.ts` seeds them with NO script change (head slot already exists). +7 tests. **Post-merge op (RAN):** `pnpm tsx scripts/seed-shop-avatar-items.ts` (the 2 new heads are live in prod). Existing children stay `gender=null` → neutral head (now with ears).

**Landmine:** *The 3 default heads (warm/boy/girl) all live on the `head` slot — `getEquippedAvatar` must pick ONE by gender, or they collide.* `listDefaultAvatarItems()` returns all three (all `unlock_via='default'`); the generic `defaultBySlot[slotId]=item` loop would let the last one win arbitrarily. `getEquippedAvatar` special-cases the head slot: among default heads it keeps only the one whose `unlockRef === defaultHeadForGender(child.gender)`, with a fallback to any head if the gendered row isn't seeded. Don't revert to the generic loop for the head slot. `gender` is plain text (validated app-side, like `avatar_items.theme`), not a pgEnum.

- **Limited-card art + Blob-token fix (PR #99, merged 2026-06-13)** — the art generators (`generate-collectible-art-cloudflare.ts`, `generate-voyage-map-art.ts`) failed ALL Blob uploads with `Access denied` even with a valid RW token, because under **dotenvx injection `@vercel/blob`'s implicit token resolution gets a stale value** (`dotenv` `config()` defaults to `override:false`, so a pre-injected old token wins). Fix: pass `token: process.env.BLOB_READ_WRITE_TOKEN` **explicitly** to `put()`. With that, generated + uploaded **the 4 season-summer-v1 card images** (`collectible_items.image_url`, render via `CardArt`) **and the 2 voyage-map backdrops** (`maps/{slug}.jpg` — `map-boards.ts` already referenced the deterministic URLs, so the illustrated home maps are now live). **Landmine:** *any future `put()` in a script must pass `token:` explicitly* — implicit env resolution is unreliable under the project's dotenvx setup.

- **Elevated shard-swap cost for limited cards (PR #100, merged 2026-06-13)** — David: protect the festival/season cards' play-to-earn exclusivity + FOMO. New pure `src/lib/economy/shards.ts` (client-safe, **shared by server + client so charged cost == displayed cost**): `SHARD_SWAP_COST=3` (regular packs), `SHARD_SWAP_COST_EXCLUSIVE=12`, `shardSwapCostForPack(slug)` keyed on `SHARD_SWAP_EXCLUSIVE_PACKS` (= `festivals-v1` + `season-summer-v1`, the `gacha_eligible=false` packs). `swapShardsInTx` derives the cost from the TARGET item's pack (joins `collection_packs`); `PackPageBody` shows the per-pack cost (`需 12🔹` on festival/season). Earn side unchanged (1 dupe → 1 shard). No migration. **Landmine:** *adding a new reward-only pack → add its slug to `SHARD_SWAP_EXCLUSIVE_PACKS`* or its cards become cheaply (3-shard) swappable, defeating the FOMO. The set is a SECOND source of truth parallel to `gacha_eligible` — keep them aligned.

- **看图找字 real pictures + ×3, easier 图和词配对 (PR #102, merged 2026-06-14)** — David: 看图找字 "图片缺失" + too few; 图和词配对 too hard. **Diagnosis:** 看图找字 = the **`image_pick`** scene; it rendered the character's `imageHook` TEXT card, not a picture, because **`characters.image_url` is 0/96** (only the 426 *word* images were ever generated — word images all reachable). **Part 1 fix:** `image_pick` now **reuses the word pictures** (David's call — robust, no generation, dodges abstract single-char AI art): `SceneRunner` passes a stimulus image from one of the target char's words (`c.words.find(w => w.imageUrl)`); `ImagePickScene` renders `<img>`, falling back to the imageHook text card. Count **1 → 3** per 10-char week. **Part 2 (image_word scaffolds):** each word option **highlights the week's learned chars** (`weekChars` = pool char hanzi) inside the word (amber) + has a per-option **🔊** to hear the word (`useSpeak`/`useSpeechSupported`; updated in #104 — see below). No schema change. **Post-merge op (RAN):** `pnpm tsx scripts/recompile-all-weeks.ts` — full weeks now 26 levels (10 review + 15 practice + 1 boss), small weeks 20.

- **Scene fix: stable option order + 🔊 audio not pinyin (PR #104, merged 2026-06-14)** — David playtest. **(1)** Options jumped mid-selection (regression from #102): `image_pick` got `target={{...c, imageUrl}}` (new object every render) + its choices memo depended on `[pool, target]`; `image_word`'s memo depended on freshly-built `distractors`/`weekChars` arrays — both reshuffled on any parent re-render. Fixed: `SceneRunner` passes a STABLE `target` + a separate `imageUrl` prop; both scenes shuffle ONCE keyed on a stable id (`target.characterId` / `correctWord.wordId`). **(2)** image_word's pinyin line replaced with a per-option **🔊** (a `role=button` span + `e.stopPropagation()` so it pronounces via `useSpeak` WITHOUT selecting; `data-testid="speak-<word>"`, generic aria-label so it doesn't pollute the option's accessible name; hidden when `useSpeechSupported()` is false). `words.pinyinArray` threading removed. Runtime-only.

**Landmine:** *Scene option shuffles MUST be keyed on a stable id, never on a prop object/array identity.* `useMemo(() => shuffle(...), [pool, target])` reshuffles whenever the parent re-renders with a new array/object (which `SceneRunner` does every render — it builds the props inline). Each scene is `key={currentLevel.id}` (fresh mount per scene), so shuffle once via `useMemo(..., [stablePrimitiveId])` + `// eslint-disable-next-line react-hooks/exhaustive-deps` (id = `characterId` / `correctWord.wordId`). The 🔊 inside an option is a `role=button` span (NOT a nested `<button>` — invalid) with `stopPropagation`; give it a GENERIC aria-label (a per-word label leaks into the parent option button's accessible name and breaks `getByRole({name})` queries).

**Landmine:** *Practice composition + counts changed (PR #102) — supersedes older "13/14 practice" notes.* `computePracticeSizing` (compile-week.ts) now returns `{ audio, imagePick, lianliankan, imageWord, meaning }` (the old generic `sight` field is gone). 10-char week = `audio 3 + imagePick 3 + lianliankan 1 + imageWord 2 + meaning 6 = 15 practice`; `PRACTICE_SCENE_COUNT=15`, `BOSS_UNLOCK_PRACTICE_THRESHOLD=7` (configs.ts). **`image_pick` eligibility is now "char has a word" (a picture to show), NOT "char has an imageHook"** — it emits whenever the char has words, and the runtime shows a word picture (or the imageHook text fallback). `characters.image_url` stays unused/0 by design (single chars are too abstract to AI-generate well; reuse the word images instead). Any compile-count change needs `recompile-all-weeks.ts` post-merge (stable level keys preserve `scene_attempts` linkage; new `image_pick:1/2` slots are added).

**Polish push (2026-06-14, all MERGED + live):** pronunciation audio (#107 — 522 CF-MeloTTS clips for every word+char in `audio_url`; `useSpeak(text, audioUrl?)` prefers the clip, TTS fallback), 🔊 unified to sky (#108), Map 2 prep (#106), unified-art tooling (#109 + this PR). **`UNIFIED_ART_STYLE`** (`src/lib/ai/art-style.ts`) is the one cartoon recipe shared by the word + collectible art generators (David approved it 2026-06-14).

- **Character pronunciation fix — revert single-char clips to device voice (PR #120, merged 2026-06-19)** — David's playtest: "很多汉字发音都不对." Root cause: MeloTTS (#107) produces **unreliable single-syllable tones** for single-character clips (`audio/chars/{id}.mp3`) — a tonal-language word on the wrong tone is the wrong word — across many of the 96 char clips (NOT just the 7 多音字 in the curriculum; the mapping is sound, the audio itself is off). Fix: `useSpeak` ignored char clips and fell back to the device's context-aware zh-CN voice. **Word clips were left on at this point** (assumed multi-syllable MeloTTS was fine) — that assumption was WRONG; see the next entry.

- **ALL MeloTTS clips disabled — word clips were also wrong (PR #125, merged 2026-06-20)** — David's follow-up playtest: "很多词的发音也都不对" (single chars fixed by #120, but words still wrong). Root cause is the SAME defect, not char-specific: **MeloTTS Mandarin is just unreliable** — wrong tones AND wrong polyphone (多音字) readings — and it's no better on multi-syllable words than on single 字. The id→text→clip→audio_url mapping is sound (verified in `generate-audio-cloudflare.ts`: clip generated from the row's own `text` with `lang:'zh'`, written back to the same id); the **audio content itself is mispronounced**. Fix: generalized #120's chokepoint — `useSpeak.ts` flag `USE_CHARACTER_CLIPS`→**`USE_MELOTTS_CLIPS = false`** and the filter `usableAudioUrl()` now drops BOTH paths via `MELOTTS_CLIP_PATH = /\/audio\/(chars|words)\//` → null → the device zh-CN voice everywhere. **Why device TTS is strictly safe here:** every call site passes the CORRECT word/char as the `text` arg (`target.hanzi`, `correctWord.text`, `w.text`, `q.target.nameZh`, `data.firstWord`…), and the device voice speaks `text` — so it can NEVER play "the wrong word," only at worst a slightly less-stylized-but-correct rendering (excellent on the target iPads — the pre-#107 voice David liked). All clip data preserved in DB/Blob behind the flag. No DB change, no recompile, no Blob ops. **Landmine:** *ALL pre-generated MeloTTS clips (chars + words) are intentionally OFF — `usableAudioUrl` in `useSpeak.ts` is the ONE chokepoint (used by both `useSpeak` and `SpeakButton`).* Do NOT "re-enable the nice consistent clips" — they are mispronounced, and consistency of a wrong tone is worse than a correct device voice. To add a verified-correct audio provider later: write its clips to a NEW Blob path (e.g. `audio/v2/…`) NOT `audio/words|chars/`, so `MELOTTS_CLIP_PATH` keeps the bad clips filtered while the new path passes through; flipping `USE_MELOTTS_CLIPS` true would un-filter the KNOWN-BAD clips and must not be the enablement mechanism. **General audio rule for future dev:** a 🔊 must always be driven by the correct `text`; treat any pre-recorded clip as an *optimization over* the device voice that has to be quality-verified per locale before shipping — never trust a TTS provider's Mandarin tones sight-unheard.

- **Per-map chrome accent (PR #123, merged 2026-06-19)** — each sea region's small shared chrome (home `MapHeaderPill`, `/maps` `MapCard`) now carries a per-map accent colour so 印度洋 doesn't echo 加勒比海's turquoise. `MapAccent` ({pillBg, pillText, cardBorder}) + `getMapAccent(slug)` (default = ocean turquoise) in `map-boards.ts`; Indian Ocean = warm spice coral/amber. Applied **inline** (plain hex, dynamic-safe). The voyage-board frame stays the shared treasure-map look by design — a map's identity comes from its backdrop + stops, this is just the chrome accent. `MapHeaderPill` now needs `currentMap.slug`. Subtle right now (Caribbean is the active map; the accent shows most once Map 2 is live). +3 tests. No DB, no migration.
- **Festival card art (PR #122, merged 2026-06-19)** — the last weak-emoji pack `festivals-v1` (12 reward cards) got real CF-flux art. Pure data backfill: `FestivalCard` already renders via `CardArt`, so just added `festivals-v1` (+ a 12-entry `FESTIVAL_SUBJECT` prompt recipe) to `generate-collectible-art-cloudflare.ts` and ran it (non-FORCE → only the 12 NULL-image rows, 12 Blob puts, 0 failed). `verify-collectible-images.ts` now tracks all 78 collectibles (62 main + 4 season + 12 festival) → **78/78 real images**. No app/UI change, no migration. Collectible art is now 100% complete across every non-intentional-emoji pack (zodiac SVG + flags real-flag-emoji stay by design).

**✅ Art-finish COMPLETE (2026-06-19).** All unified-style art is done: **word images 426/426** (`verify-image-url-column.ts` → 0 NULL) and **collectible cards 78/78** (62 main packs + 4 season-summer-v1 + 12 festivals-v1, via `verify-collectible-images.ts`). The final 9 NSFW-flaky stragglers (words 花生/宝石/不好/儿子; cards seashell/oyster/sea-otter/neptune/uranus) were regenerated on 2026-06-19 once CF quota cleared, via the resume-flagged commands (`SKIP_UPLOADED_AFTER=2026-06-15 …regenerate-word-images-cloudflare.ts` → 4 regen/422 skip; `FORCE=1 SKIP_UPLOADED_AFTER=2026-06-18 …generate-collectible-art-cloudflare.ts` → 5 regen/61 skip). **Both scripts retain the `SKIP_UPLOADED_AFTER` RESUME flag** — any future re-run MUST use it (skips already-done assets via a free HEAD, no neurons / no wasted Blob `put`). Per-slug NSFW overrides live in each script's `SUBJECT_OVERRIDE`/`SOLAR_SUBJECT` (e.g. avoid the literal word "Uranus" — trips flux). Voyage MAPS keep their own landscape recipe (not single-subject) — leave them. CF free tier ≈ 300 flux images / UTC day (10k neurons ÷ ~33). David stays on free tier — **never bulk-regenerate**; only generate genuinely missing/changed assets, always with the resume flag.

- **4 KS1 vocab packs + Study Mode (PR #124, merged 2026-06-20; gong art fix #126)** — David's "enrich the Backpack with school topics" ask. **(A) 4 new bilingual collectible packs** aligned to UK Key Stage 1: 🚒 `transport-v1` (14, grouped 陆地/水上/天空) · 🦋 `minibeasts-v1` (12, flat) · 🎻 `instruments-v1` (13, grouped 西洋/民族, incl. 二胡/琵琶/古筝 as a culture bridge) · 🦊 `animals-v1` (17, flat). **Animals DELIBERATELY excludes all 12 zodiac animals** (a test asserts it) — no overlap with `zodiac-v1`. Regular **gacha-eligible** packs (flow through boss/perfect/gift grants like the other 8). New shared **`makeVocabCard`** factory (`src/components/play/items/VocabCard.tsx`) → 4 thin wrapper cards (3 lines each); 4 data files `src/lib/collections/{transport,minibeasts,instruments,animals}Data.ts`; 4 `PACK_REGISTRY` entries; one `scripts/seed-vocab-packs.ts` (seeded all 56 cards to prod) + one `buildPrompt` recipe edit in `generate-collectible-art-cloudflare.ts`. **Collectible art is now 134 cards (78 + 56), all real** (gong/锣 needed a `SUBJECT_OVERRIDE` for flux's NSFW false-positive — PR #126). **(B) Study Mode** — a 📖 学习 button on every pack page (enabled at ≥3 owned, else "收集 3 张即可学习"); tapping → `/play/[childId]/collection/[packSlug]/study` → a ~6-question **picture+audio+meaning** lesson (看图选词 + 听音选图) built **from the cards the child OWNS** (`buildStudyLesson` pure in `src/lib/play/study.ts`; questions built server-side, passed as plain data). Finishing with a passing score (≥60) gifts **1 card from that pack, once per (pack, UTC day)** via a new `'study'` card source — pack-scoped pull (`pullCardForChild(childId,'study',`​`${packSlug}:${dayUtc}`​`,packSlug)`), still under the shared 10/day cap, **anti-farm gated on the grant** (mirrors homework — XP+card fire only on `granted`). Device-TTS audio (no generated clips). `StudyRunner` reuses `MultipleChoiceQuiz` + `CardChestReveal`. New `'study'` in `CardGrantSource` + `XpSource` + `pullCardInTx` union; new optional `packSlug` scoping on `pullCardInTx`/`pullCardForChild`. **No DB migration, no recompile** (data rows + `text` source columns). +~40 tests.

**Landmine:** *Adding a collectible pack = the `landmarks-v1` recipe (data file + a `makeVocabCard` wrapper or bespoke card + `PACK_REGISTRY` entry + seed script + `buildPrompt` case), NO migration.* For a simple bilingual emoji-or-art pack, prefer the shared `makeVocabCard` factory (`VocabCard.tsx`) over a bespoke component — pass `bySlug`, `fallbackEmoji`, optional `groupLabels`, `testId`. Art: append the slug to `TARGET_PACK_SLUGS` + a `buildPrompt` case, run the generator **non-FORCE** (only fills rows whose `image_url` isn't http — seed writes the emoji there as the text fallback, so new cards are picked up and existing ones skipped). Make the pack `gacha_eligible=true` unless it's reward-only (then add it to the festival/season exclusions too). *Study Mode reward mirrors the homework/section card mechanic* — `'study'` source, refId `${packSlug}:${todayUtcIso()}`, reward gated on the `granted` branch (the card-grant log is the single idempotency source); reward-only packs (`gacha_eligible=false`) never grant a study card. `pullCardInTx`'s new optional `packSlug` scopes the catalog to one pack — don't pass it for the un-scoped (any-pack) grant sources.

**Landmine — Vercel Blob free tier = 2,000 Advanced Operations / month (rolling, auto-resets each cycle); blocked at 100% until it rolls over.** This is a HARD constraint — David keeps this a hobby project on the free tier (no Pro). What counts against the 2,000: **`put()` (every upload), `copy()`, `list()`, AND browsing the Blob store in the Vercel dashboard.** What does NOT: `head()` + gameplay reads (Simple Operations / Edge Requests — much larger separate budgets, ~100k+/mo), `del()` (free). **The running app does ZERO `put`s during gameplay** — the only app upload path is `src/lib/ai/pollinations.ts` `put()`, fired only when David authors a NEW week (~14 word images). So normal play never threatens the cap; **100% of the drain is our one-off generation scripts** (`generate-audio-cloudflare` 522 puts, `regenerate-word-images-cloudflare` ~426, `generate-collectible-art-cloudflare` ~66, etc.). **RULES:** (1) **Generate each asset ONCE; NEVER bulk-regenerate** — a full 426-word redo ≈ 426 advanced ops ≈ 21% of the monthly cap; doing it twice in a month (as on 06-14 + 06-16) is the cardinal waste. (2) **Every re-run MUST use the resume flag** (`SKIP_UPLOADED_AFTER` for words; add an equivalent `head()`-based skip to any new put-heavy script) so already-done assets are skipped via cheap `head()` instead of re-`put()`. (3) Only generate genuinely MISSING assets going forward (the authoring path already does this — `put`s only words lacking `image_url`). (4) **Don't click around the Blob store in the dashboard** — use the Observability page sparingly; browsing is billed as Advanced Operations. (5) If a big re-style is ever truly needed again, spread it across month boundaries or use a short free Pro trial — never burn two bulk runs in one cycle. Steady state (play + occasional new week) sits FAR under 2,000.

- **Distribution-readiness audit (PR #112, merged 2026-06-16)** — first slice of "harden for distribution" (audience: **friends & family**, David's pick). An isolation + hardcode audit, **no DB migration / no recompile / no game-loop change**. (1) **Closed a cross-account write hole:** `pullCardForChild` + `claimWeeklyGiftIfDue` were exported from the `'use server'` file `actions/gacha.ts` → **public RPC endpoints** accepting a raw `childId` with no ownership check. Relocated them (+ shared helpers `safePackCompleteTrophy`/`safeContinentTrophies`/`addDaysUtc` + `CardGrantSource` type) into a plain server-only module **`src/lib/play/card-grants.ts`** (NOT `'use server'`); gated actions (`finishLevelAction`/`finishAttemptAction`/`finishHomeworkAction`/`generateStoryChapter`) still import + call them. Deleted the dead `triggerEagerStoryGeneration` endpoint (unused since Story hidden #93). Gated `generateMissingImagesForWeek` with `assertParent()` (was logged-in-only). (2) **Read side verified safe** (no change): `play/[childId]/layout.tsx` `requireChild` guard covers every play subpage; parent reads use `requireChild`/`getChildOwnedBy`. (3) **Hardcode generalization:** removed the 2 user-visible family strings (landing badge + parent blurb "海盗班 crew"); generalized the Story hero name (`deepseek-story.ts` `TONE_INSTRUCTIONS` + `LatestChapterPill`) "Yinuo"/"she" → "the hero"/"they" (forward-looking; Story stays hidden, prompt already threads `child.displayName`); swept ~13 internal "Yinuo"/"海盗班" doc comments. (4) **Minimal multi-child picker** (`src/components/ChildPicker.tsx`, prop `players`) on the landing for >1-child accounts (single-child redirect + zero-child landing unchanged). (5) **Regression net** `tests/unit/distribution-isolation-guard.test.ts` (source-level: relocated fns not exported from `'use server'`; `card-grants.ts` not `'use server'`; no "海盗班"/"Yinuo" in `src/app`+`src/components`). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-16-distribution-readiness-audit*`.

**Landmine:** *Every exported `async function` in a `'use server'` file is a PUBLIC RPC endpoint* — the client can call it with any args, regardless of the "intended" caller. A "trust-caller" helper that takes a `childId`/`weekId` and skips `requireChild`/`assertParent` is a cross-account hole even if it's only *called* from gated actions. Fix pattern (PR #112): put such helpers in a plain server-only module (e.g. `src/lib/play/card-grants.ts`) that gated actions import — NOT in `src/lib/actions/*`. `tests/unit/distribution-isolation-guard.test.ts` guards this (asserts `pullCardForChild`/`claimWeeklyGiftIfDue`/`triggerEagerStoryGeneration` aren't `'use server'` exports + no family strings rendered). When adding any new child-scoped server action, it MUST `requireChild`/`assertParent` at its entry, or live as a non-action helper.

- **Admin grant console (PR #117, merged 2026-06-17)** — an admin/test account ("god mode") that grants ANY child (across accounts) anything obtainable in-game. New **`assertAdmin`** guard (`src/lib/auth/guards.ts`; role must be exactly `'admin'` — the `user_role` enum already had it; `assertParent` still allows parent OR admin). **`banbanhu4ever@gmail.com` (Ban Ban, owns 小板) is now `role='admin'`** (set via `scripts/set-admin-role.ts <email> [admin|parent]`). New admin-only `/admin` route (`src/app/admin/layout.tsx` does `assertAdmin`→`notFound`); dashboard link rendered only when `parent.role==='admin'`. Console: pick a child → compose a **custom gift bundle** (`GiftBundleSchema` in `src/lib/admin/bundle.ts`: coins ±, xp, shards, powerups, specific cards, `giftPack`, `shopItemIds`, `shopUnlockAll` per-category) → `sendAdminGiftAction(childId, bundle)`; logged to new **`admin_grants`** table (migration 0031: `bundle` + concrete `result` jsonb + `undone_at`) → `undoAdminGiftAction(grantId)` reverses exactly `result` (clamped ≥0). Grant primitives in `src/lib/db/admin-grants.ts` (`grantShardsInTx`/`grantPowerupInTx`/`grantSpecificCardInTx`/`removeCardInTx`/`revokeShopItemInTx`); shop-ownership grant reuses a free path `applyShopItemOwnershipInTx` refactored out of `purchaseShopItemInTx` (coins via `awardCoins` reason `admin_adjust`, xp via `awardXp` source `admin_grant`, gift-pack via `grantGiftPackInTx` with an `admin:{id}:{ts}` refId). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-16-admin-grant-console*`. **Known limitation:** *XP undo is a no-op* (`awardXp` rejects ≤0 and the bundle won't grant negative xp) — undo reverses coins/shards/powerups/cards/shop but leaves granted XP (XP is cosmetic/season-derived). **Regression caught + fixed in-PR:** the `purchaseShopItemInTx` refactor reordered the powerup path to debit before validating `powerupKind` — fixed by `resolvePowerupKind()` validating up front (Task 3's gate hadn't run `purchase-powerup.test.ts`).

**Landmine:** *`assertAdmin` is the sanctioned cross-account write path — admin grant actions deliberately SKIP `requireChild`.* This is the ONE place a server action legitimately mutates another account's child data; it's safe ONLY because `assertAdmin` requires `role==='admin'` (a 4-account hobby deployment, manually promoted). Every admin action (`src/lib/actions/admin.ts`) calls `assertAdmin()` as its first statement and the `/admin` layout guards rendering — never add a child-scoped admin action without `assertAdmin`, and never broaden `assertAdmin` to allow `'parent'`. The PR #112 isolation guard test checks specific function names, so the admin actions (a named, role-gated exception) don't trip it. Promote/demote with `scripts/set-admin-role.ts`.

- **Child gender editable + Yinuo set to girl (PR #116, merged 2026-06-16)** — `updateChildAction` now threads `gender` (it only handled name+birthYear; gender was creation-only since #97); `EditChildForm` gained a 男孩/女孩/未设置 radio. Gender drives the default-head avatar (#97). Yinuo's profile set to `gender='girl'` directly in prod (her avatar = the girl default head; the boy/girl difference is face-only by David's choice — no distinct hairstyle).

- **Login Kid/Parent chooser — "remember last choice" (PR open 2026-06-19)** — David: after sign-in, let the user **choose** the kid game (no PIN) or parent control (PIN-gated) rather than always auto-jumping into the game. Replaces PR #38's unconditional single-child auto-redirect with a **fork that remembers the last pick**. New pure `src/lib/auth/entry-pref.ts` (`ENTRY_COOKIE='hq_entry'`, `parseEntryPref`, `kidEntryValue` — client-safe, shared by reader+writer). New `'use server'` `src/lib/actions/entry.ts`: `chooseKidEntryAction(childId)` (ownership-validated → sets cookie `kid:<id>` → redirect `/play/<id>`) + `chooseParentEntryAction()` (cookie `parent` → redirect `/parent`, where the `(secured)` PIN gate still applies). New server-component `src/components/EntryChooser.tsx` (forms bound to the actions; single child = one big 🎮 开始游戏 button, multi = per-child list, 0 = parent-only; bilingual). Root `/` (`page.tsx`) now: signed-out → landing; signed-in → if a valid `hq_entry` cookie exists (and `?choose=1` absent) auto-redirect there (kid pref re-validated against current children), else render `EntryChooser`. A `切换 / Switch` link in the parent `(secured)` header points to `/?choose=1` to re-open the fork. **`ChildPicker` deleted** (EntryChooser absorbs multi-child picking). Cookie is httpOnly, 1-yr, lax. +13 tests; root-page test rewritten for the new pref routing. No DB change, no recompile. **Landmine:** *the root `/` is now pref-driven, not "single-child = auto-redirect."* First login (no `hq_entry` cookie) ALWAYS shows the chooser, even for one child; the redirect only fires when a valid pref cookie exists. A `kid:<id>` pref is re-validated against `listChildrenForUser` each load (stale child → fall back to chooser, no redirect). `?choose=1` forces the chooser past the cookie. Don't restore the old `children.length===1 → redirect` shortcut — it bypasses the fork.

- **Shop purchase feedback (PR #128, merged 2026-07-01)** — David: buying in the shop gave no confirmation + re-tapping an owned item threw a scary red error (no coins were ever lost — ownership is checked before the debit). Fix: `purchaseShopItemAction` now **returns a discriminated `PurchaseOutcome`** (`{status:'purchased',trophies}` | `{status:'already_owned'}` | `{status:'insufficient',required,available}`) instead of throwing — thrown errors lose their class across the server-action RPC boundary, so the client couldn't `instanceof` reliably (unexpected errors still throw). New shared `useShopPurchase(childId)` hook + `ShopToast` (auto-dismiss ~2s, bilingual: ✅ 已购买 / 👍 你已经拥有了 / 🪙 金币不够啦). All 5 tab bodies + the avatar Buy bar route purchase feedback through the toast (dropped the red banner); Decor keeps its `TrophyToast` via an `onPurchased(trophies)` callback; powerups stay repeatably buyable. +14 tests. **Landmine:** *a `'use server'` action's thrown error loses its class on the client* — for expected/branchable outcomes (already-owned, insufficient) RETURN a discriminated status, don't throw; reserve throws for unexpected errors.

- **Map Final Boss / 海域霸主 (PRs #129 core + #130 gating, merged 2026-07-02)** — a culminating multi-phase battle at the end of each map. Clear **every week** in a map → a **👑 lair node** appears at the end of the voyage board → a 3-phase gauntlet (`FinalBossScene`, 3 ⚓ lives, ~18 Qs sampled across ALL the map's characters via pure `buildFinalBossPhases`, **full restart on loss**) vs. a per-map **overlord** (Caribbean = **幽灵旗舰 / Ghost Galleon**, `getFinalBoss(packSlug)` client-only registry; bigger procedural-SVG, `LazyMotion`, reduced-motion safe). Win → **champion bundle**, idempotent-once: a reward-only **`champions-v1`** card (`gacha_eligible=false`, shard-exclusive; `champion-caribbean`), a **champion trophy** (new `champion` trophy_category — a **pgEnum**, so migration `0033` `ALTER TYPE ADD VALUE`), and an auto-equipped **crown cosmetic** (`theme:'champion'`, `rewardOnly`, in `REWARD_THEMES`). New `final_boss_clears` table (migration `0032`, PK child+pack) is the **single source of truth** for "map beaten": it drives BOTH reward idempotency (`finishFinalBossAction` — `requireChild` + anti-cheat re-verify `isMapFullyCleared` + `recordFinalBossClear` 23505-guard) AND **linear next-map gating** (`mapOrderIndex` parses `pirate-class-level-N`; `computeMapGating` in `listMapsForChild` locks a map until the previous overlord is beaten — Map 1 never gated; `switchMapAction` rejects a gated switch with reason `'gated'`). Home shows a derived **👑 加勒比海霸主 / Lord of the Caribbean** title chip (`latestChampionTitle` pure helper, from `final_boss_clears`; no equip system). Question pool built at request time (no compile). Reveal via `CardChestReveal` + `TrophyToast`. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-30-map-final-boss*`. **Post-merge ops RAN:** `seed-champions-pack.ts` (1 card, image_url NULL → `CardArt` shows 👑) · `seed-trophies.ts` (+1) · `seed-festival-avatar-items.ts` (+1 crown). Champion card CF-flux art deferred (👑 emoji is serviceable).

**Landmine:** *`final_boss_clears` is the ONE source of truth for a beaten map — it gates rewards AND the next map.* `finishFinalBossAction`'s idempotency is the `recordFinalBossClear` insert (23505 → `firstClear:false` → grant nothing); anti-cheat re-verifies `isMapFullyCleared` server-side (never trust the client "I won"). `getFinalBoss`/the final-boss roster hold React components (client-only — RSC hazard like `boss-roster`/`packRegistry`; the scene resolves them). Adding a map's overlord = a `bosses/<Name>.tsx` (`BossCreatureProps`) + a `FINAL_BOSS_ROSTER[slug]` entry + a `champions-v1` card (`MAP_TO_CHAMPION_CARD`) + a crown ItemDef (`unlockRef === card slug`) + `MAP_TO_CHAMPION_TROPHY` + `CHAMPION_TITLES` entries; then re-run the 3 seed scripts. Champion card slug === crown `unlockRef` by convention (`grantChampionCosmetic` resolves the crown by the card slug).

**Landmine:** *A test importing a `@/lib/db/*` module (or `@/lib/actions/*` that transitively loads one) MUST mock `@/db` (`vi.mock('@/db', () => ({ db: {} }))`) or it throws `DATABASE_URL is not set` — but ONLY on CI (local `.env.local` has it, so it passes locally and fails only in CI).* This bit the `champion-trophy` test (imports `@/lib/db/trophies`) in #129. Separately: **always run the FULL local suite before pushing, not just the new test files** — changing a shared read like `listMapsForChild` (added a `listFinalBossClears` query) broke the EXISTING `lib/db/maps.test.ts` db-chain mock, which the new-files-only run missed and CI caught (#130). Both are avoidable with `pnpm test` (full) pre-push.

- **Answer-event telemetry + flashcard self-assessment (PR open 2026-07-03, A1 of `docs/IMPROVEMENT-ROADMAP.md`)** — the game's first per-item learning memory. New **`answer_events`** table (migration 0034, write-only in v1): one row per answered question with `source` (review/practice/boss/homework/study), `scene_type`, `character_id`/`word_id`/`item_key`, `correct`, `self_rating`, `picked_key` (which wrong choice was tapped — future confusion-pair analysis). Events ride the EXISTING finish actions as an optional `events` field (`finishAttemptAction` also gained `source`); inserted AFTER primary writes via `logAnswerEventsSafe` (`src/lib/db/answer-events.ts`, never throws, drops invalid elements, caps 40/call). Client: `src/lib/play/answer-events.ts` (client-safe types + zod, exactly-one-of `correct`/`selfRating`); `MultipleChoiceQuiz` gained `onResult` (fires at tap time); the 5 MCQ scenes + `BossScene` (per-question, no pickedKey) + `HomeworkRunner` + `StudyRunner` emit via `onAnswerEvent`; `SceneRunner` batches per level in `eventsRef` (captured+cleared synchronously in `advance`). **Review flashcards now end in 3 bilingual self-assessment buttons — 认识 / Got it · 不确定 / Not sure · 不认识 / Don't know** (replaced the lone "Got it →"); ALL three advance as success (score 100 — honesty never punished), only the logged rating differs. Deferred: lianliankan/word_match pair logging, final-boss wiring, all reads (A2 review loop + A3 parent insights build on this). +~30 tests (1543 → 1590). Spec: `docs/superpowers/specs/2026-07-03-answer-events-and-flashcard-self-assessment-design.md`.

**Landmine:** *`answer_events` is write-only telemetry — flashcard self-ratings must NEVER affect scoring/rewards.* All three rating buttons complete the review scene as success; don't gate anything on `self_rating`. Events piggyback the finish actions (optional `events` field) with `childId`/`weekId`/`source` set SERVER-side from the action's validated context — never add a standalone public log action (PR #112 endpoint rule). `logAnswerEventsSafe` swallows all errors; keep the call AFTER primary writes. `source`/`scene_type`/`self_rating` are text columns validated by zod in the client-safe module `src/lib/play/answer-events.ts` (no db imports — scenes import it). Any test importing the finish actions now needs `vi.mock('@/lib/db/answer-events', () => ({ logAnswerEventsSafe: vi.fn().mockResolvedValue(0) }))` (the mock-`@/db` landmine; bit 5 existing suites in this PR). Boss inner question scenes are deliberately NOT wired to `onAnswerEvent` — `BossScene.handleAnswer` emits the `boss_question` event itself (wiring both would double-log).

**Next up:** map final boss + shop feedback shipped (above). **`docs/IMPROVEMENT-ROADMAP.md` (2026-07-03) is the prioritized improvement plan** — answer-event telemetry (A1) shipped; A2 cross-week review + A3 parent insights are the natural follow-ups now that data is accumulating. **Map 2 (印度洋) authoring** (prepped — `scripts/seed-pirate-class-2.ts`, just needs David's 10 weeks of hanzi) is the main remaining big item. Enrichment paths brainstormed with David (spec `docs/superpowers/specs/2026-06-20-themed-vocab-packs-and-study-mode-design.md`): more KS1 packs (家庭/水果蔬菜/身体…), curriculum-aligned weekly hanzi, and writing/stroke + tone practice remain candidates. Home/shop/backpack CSS was audited and is already structurally consistent (per-pack accent is intentional). **Multi-buy furniture** is approved but PARKED (David, 2026-07-01): furniture buyable/placeable in multiples (cap **3**/item via a new `maxOwned` on `FurnitureDef`; wallpaper/floor surfaces stay 1); needs count-based ownership + dropping the `home_placements` own-1 unique for multi-instance placement — #128's `PurchaseOutcome` status is the groundwork. Story Mode redesign is PAUSED (David). ALWAYS confirm with David before starting a new PR.

